// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: distribution/search.proto

package distributionconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	distribution "github.com/ttab/elephant-public-api/distribution"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SearchName is the fully-qualified name of the Search service.
	SearchName = "elephant.distribution.Search"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SearchQueryProcedure is the fully-qualified name of the Search's Query RPC.
	SearchQueryProcedure = "/elephant.distribution.Search/Query"
	// SearchGetMappingsProcedure is the fully-qualified name of the Search's GetMappings RPC.
	SearchGetMappingsProcedure = "/elephant.distribution.Search/GetMappings"
)

// SearchClient is a client for the elephant.distribution.Search service.
type SearchClient interface {
	// Query searches one language series of one tier. Language is required
	// and a query never fans out across languages: searching several
	// languages is several calls.
	Query(context.Context, *connect.Request[distribution.QueryRequest]) (*connect.Response[distribution.QueryResponse], error)
	// GetMappings returns the searchable fields of a document type and the
	// type of each field, as the union across the language indexes. Use it
	// to find out what can be queried, and how, before building a query.
	GetMappings(context.Context, *connect.Request[distribution.GetMappingsRequest]) (*connect.Response[distribution.GetMappingsResponse], error)
}

// NewSearchClient constructs a client for the elephant.distribution.Search service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSearchClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SearchClient {
	baseURL = strings.TrimRight(baseURL, "/")
	searchMethods := distribution.File_distribution_search_proto.Services().ByName("Search").Methods()
	return &searchClient{
		query: connect.NewClient[distribution.QueryRequest, distribution.QueryResponse](
			httpClient,
			baseURL+SearchQueryProcedure,
			connect.WithSchema(searchMethods.ByName("Query")),
			connect.WithClientOptions(opts...),
		),
		getMappings: connect.NewClient[distribution.GetMappingsRequest, distribution.GetMappingsResponse](
			httpClient,
			baseURL+SearchGetMappingsProcedure,
			connect.WithSchema(searchMethods.ByName("GetMappings")),
			connect.WithClientOptions(opts...),
		),
	}
}

// searchClient implements SearchClient.
type searchClient struct {
	query       *connect.Client[distribution.QueryRequest, distribution.QueryResponse]
	getMappings *connect.Client[distribution.GetMappingsRequest, distribution.GetMappingsResponse]
}

// Query calls elephant.distribution.Search.Query.
func (c *searchClient) Query(ctx context.Context, req *connect.Request[distribution.QueryRequest]) (*connect.Response[distribution.QueryResponse], error) {
	return c.query.CallUnary(ctx, req)
}

// GetMappings calls elephant.distribution.Search.GetMappings.
func (c *searchClient) GetMappings(ctx context.Context, req *connect.Request[distribution.GetMappingsRequest]) (*connect.Response[distribution.GetMappingsResponse], error) {
	return c.getMappings.CallUnary(ctx, req)
}

// SearchHandler is an implementation of the elephant.distribution.Search service.
type SearchHandler interface {
	// Query searches one language series of one tier. Language is required
	// and a query never fans out across languages: searching several
	// languages is several calls.
	Query(context.Context, *connect.Request[distribution.QueryRequest]) (*connect.Response[distribution.QueryResponse], error)
	// GetMappings returns the searchable fields of a document type and the
	// type of each field, as the union across the language indexes. Use it
	// to find out what can be queried, and how, before building a query.
	GetMappings(context.Context, *connect.Request[distribution.GetMappingsRequest]) (*connect.Response[distribution.GetMappingsResponse], error)
}

// NewSearchHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSearchHandler(svc SearchHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	searchMethods := distribution.File_distribution_search_proto.Services().ByName("Search").Methods()
	searchQueryHandler := connect.NewUnaryHandler(
		SearchQueryProcedure,
		svc.Query,
		connect.WithSchema(searchMethods.ByName("Query")),
		connect.WithHandlerOptions(opts...),
	)
	searchGetMappingsHandler := connect.NewUnaryHandler(
		SearchGetMappingsProcedure,
		svc.GetMappings,
		connect.WithSchema(searchMethods.ByName("GetMappings")),
		connect.WithHandlerOptions(opts...),
	)
	return "/elephant.distribution.Search/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SearchQueryProcedure:
			searchQueryHandler.ServeHTTP(w, r)
		case SearchGetMappingsProcedure:
			searchGetMappingsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSearchHandler returns CodeUnimplemented from all methods.
type UnimplementedSearchHandler struct{}

func (UnimplementedSearchHandler) Query(context.Context, *connect.Request[distribution.QueryRequest]) (*connect.Response[distribution.QueryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("elephant.distribution.Search.Query is not implemented"))
}

func (UnimplementedSearchHandler) GetMappings(context.Context, *connect.Request[distribution.GetMappingsRequest]) (*connect.Response[distribution.GetMappingsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("elephant.distribution.Search.GetMappings is not implemented"))
}
