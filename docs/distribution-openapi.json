{
  "components": {
    "schemas": {
      "elephant.distribution.ActivateConfigGenerationRequest": {
        "properties": {
          "id": {
            "description": "ID of the generation to activate.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ActivateConfigGenerationResponse": {
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.ConfigGeneration"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ActivateIndexGenerationRequest": {
        "properties": {
          "force": {
            "description": "Force activates a generation that lags the head of the eventlog by more than the gate allows.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the generation to activate.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ActivateIndexGenerationResponse": {
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.IndexGeneration"
          },
          "handover_position": {
            "description": "HandoverPosition is the eventlog position the outgoing generation had matched subscriptions up to. The incoming generation replays matching from there, so it is also the lower bound of the overlap the two generations may both have matched - and therefore of the window a subscription can see the same document version delivered twice.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ArchiveChunk": {
        "properties": {
          "chunk_id": {
            "description": "ChunkID is the sequential chunk identifier.",
            "type": "integer"
          },
          "events_url": {
            "description": "EventsURL is a presigned GET URL for the chunk event zip.",
            "type": "string"
          },
          "first_event_at": {
            "description": "FirstEventAt is the timestamp of the first event, RFC3339.",
            "type": "string"
          },
          "first_event_id": {
            "description": "FirstEventID is the first eventlog ID in the chunk.",
            "type": "integer"
          },
          "last_event_at": {
            "description": "LastEventAt is the timestamp of the last event, RFC3339.",
            "type": "string"
          },
          "last_event_id": {
            "description": "LastEventID is the last eventlog ID in the chunk.",
            "type": "integer"
          },
          "manifest_hash": {
            "description": "ManifestHash is the hex-encoded sha256 of the canonical manifest.",
            "type": "string"
          },
          "manifest_url": {
            "description": "ManifestURL is a presigned GET URL for the signed chunk manifest.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ArchivedDocument": {
        "properties": {
          "content_url": {
            "description": "ContentURL is a presigned GET URL for the canonical document JSON.",
            "type": "string"
          },
          "hash": {
            "description": "Hash is the hex-encoded sha256 of the canonical document body, for a pre-download integrity check. The document object's own signature (the authoritative verification anchor) is delivered with the object.",
            "type": "string"
          },
          "uuid": {
            "description": "UUID of the document.",
            "type": "string"
          },
          "version": {
            "description": "Version is the latest archived version, or -1 if the document has been tombstoned (in which case there is no content URL).",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.BoolQuery": {
        "description": "BoolQuery combines subqueries. Must and should contribute to the score, filter and must_not don't.",
        "properties": {
          "filter": {
            "description": "Filter are subqueries that all have to match, without affecting the score of the hit.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Query"
            },
            "type": "array"
          },
          "minimum_should_match": {
            "description": "MinimumShouldMatch is the number of should subqueries that have to match. Defaults to 1 when the query has no must or filter subqueries, 0 otherwise.",
            "type": "integer"
          },
          "must": {
            "description": "Must are subqueries that all have to match.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Query"
            },
            "type": "array"
          },
          "must_not": {
            "description": "MustNot are subqueries that must not match.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Query"
            },
            "type": "array"
          },
          "should": {
            "description": "Should are subqueries that may match, see minimum_should_match.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Query"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CalibrateVectorClauseRequest": {
        "properties": {
          "document_types": {
            "description": "DocumentTypes to score against. Empty scores against every indexed type the caller can read. Use the types the subscription is going to name, the volume estimate is only about the documents it will see.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "fields": {
            "description": "Fields to return with each top hit, named as flat index fields (e.g. \"document.rel.section.uuid\"), as in Search.Query. Only the top hits carry them - they are for reading the results, and the rest of the response is counts.\n The title is returned whether or not it is selected, so eyeballing the top hits needs nothing here; this is for the other things that say what a document is.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "floor": {
            "description": "Floor is the similarity below which documents are not scored at all, which bounds the work the call does. Documents below it are counted as non-matches at every rung of the ladder, so a floor above a candidate threshold would understate its volume and is an invalid argument. Absent uses a server-chosen floor.",
            "type": "number"
          },
          "language": {
            "description": "Language to score against, as the language tag documents are indexed under. Empty scores against every language, matching a subscription that leaves the language out.",
            "type": "string"
          },
          "members": {
            "description": "Members to calibrate, scored the way a clause scores them: each document gets the highest similarity any of the members reaches. Pass the members of one clause to calibrate it as a whole, or a single member to calibrate it on its own. At least one is required.\n The min_similarity of a member is not used for scoring. When it is set it is added to the threshold ladder, so an existing threshold can be compared against the alternatives.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.VectorClauseMember"
            },
            "type": "array"
          },
          "thresholds": {
            "description": "Thresholds is an explicit ladder of candidate thresholds to estimate volume at. Empty lets the server pick a ladder from the observed distribution, which is the normal way to use this.",
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "top_hits": {
            "description": "TopHits is the number of highest-scoring documents to return for eyeballing. Defaults to 10, and is capped by the server.",
            "type": "integer"
          },
          "window_days": {
            "description": "WindowDays is the number of days back to score. Defaults to 7 and is capped by what the recent tier holds. A window shorter than a week will mistake the weekly publishing rhythm for a trend.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CalibrateVectorClauseResponse": {
        "properties": {
          "floor": {
            "description": "Floor that was applied.",
            "type": "number"
          },
          "percentiles": {
            "description": "Percentiles of the similarity distribution over the window, in ascending order of percentile.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.SimilarityPercentile"
            },
            "type": "array"
          },
          "scored_documents": {
            "description": "ScoredDocuments is the number of documents in the window that scored at or above the floor. The percentiles are over these, not over every document in the window: the distribution of things that are at least vaguely related is what a threshold is chosen within.",
            "type": "integer"
          },
          "thresholds": {
            "description": "Thresholds is the ladder: what each candidate threshold would have matched over the window. This is the number a threshold is chosen on - a threshold is a volume decision, and matches per day is the volume it produces. Nothing here says anything about whether the matches are the right ones; that is what the top hits and PreviewSubscription are for.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ThresholdEstimate"
            },
            "type": "array"
          },
          "top_hits": {
            "description": "TopHits are the highest-scoring documents of the window, most similar first. Read them to find where the results stop being about what the clause is about; the similarity there is the threshold worth estimating volume at.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.CalibrationHit"
            },
            "type": "array"
          },
          "window_days": {
            "description": "WindowDays is the window that was actually scored, which can be shorter than the requested one.",
            "type": "integer"
          },
          "window_end": {
            "description": "WindowEnd is the end of the scored window, RFC3339.",
            "type": "string"
          },
          "window_start": {
            "description": "WindowStart is the beginning of the scored window, RFC3339.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CalibrationHit": {
        "description": "CalibrationHit is one of the documents a calibration scored highest.",
        "properties": {
          "fields": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.StringValues"
            },
            "description": "Fields are the values the request's field selection asked for. The title is not among them unless the selection named it too.",
            "type": "object"
          },
          "first_published": {
            "description": "FirstPublished is when the document was first published, RFC3339.",
            "type": "string"
          },
          "member_index": {
            "description": "MemberIndex is the position in the calibrated members of the member that scored highest against this document.",
            "type": "integer"
          },
          "similarity": {
            "description": "Similarity the document scored, i.e. the highest similarity any of the calibrated members reached against any of its chunks.",
            "type": "number"
          },
          "title": {
            "description": "Title of the document, always returned. Choosing a threshold means reading down the top hits until they stop being about what the clause is about, and a list of UUIDs cannot be read that way.\n Empty for a document the index holds no title for, which is not the same as a document that isn't there.",
            "type": "string"
          },
          "type": {
            "description": "Type of the document.",
            "type": "string"
          },
          "uuid": {
            "description": "UUID of the document. Pass it to Content.GetDocument to read it.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CancelImportRequest": {
        "properties": {
          "id": {
            "description": "ID of the import job, or the upload ID of an un-finalized upload (they are the same identifier space).",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CancelImportResponse": {
        "type": "object"
      },
      "elephant.distribution.ClauseProvenance": {
        "description": "ClauseProvenance is the outcome of one vector clause.",
        "properties": {
          "clause_index": {
            "description": "ClauseIndex is the position of the clause in SubscriptionSpec.vector_clauses.",
            "type": "integer"
          },
          "members": {
            "description": "Members of the clause with the similarity each of them scored. All members are reported, not only the ones that passed, so that a near miss is visible.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.MemberProvenance"
            },
            "type": "array"
          },
          "negate": {
            "description": "Negate is whether the clause is negated, repeated here so that the provenance can be read without the subscription at hand.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ConfigGeneration": {
        "properties": {
          "activated_at": {
            "description": "ActivatedAt is the time the generation was last activated, RFC3339, or empty if it has never been activated.",
            "type": "string"
          },
          "created_at": {
            "description": "CreatedAt is the time of registration, RFC3339.",
            "type": "string"
          },
          "description": {
            "description": "Description is an optional human-readable tag.",
            "type": "string"
          },
          "id": {
            "description": "ID is assigned by the server on registration.",
            "type": "integer"
          },
          "renditions": {
            "description": "Renditions configured in the generation, one per asset kind.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionConfiguration"
            },
            "type": "array"
          },
          "schemas": {
            "description": "Schemas in the generation.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ConfigGenerationSchema"
            },
            "type": "array"
          },
          "types": {
            "description": "Types configured in the generation.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ConfigGenerationType"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ConfigGenerationSchema": {
        "description": "ConfigGenerationSchema references a schema version included in a generation. Spec is optional when the version is already stored; it is required when registering a new (name, version) pair.",
        "properties": {
          "name": {
            "type": "string"
          },
          "spec": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ConfigGenerationType": {
        "description": "ConfigGenerationType configures a document type within a generation.",
        "properties": {
          "configuration": {
            "$ref": "#/components/schemas/elephant.distribution.TypeConfiguration"
          },
          "type": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CreateImportUploadRequest": {
        "properties": {
          "files": {
            "description": "Files that make up the import set, in load order.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ImportFile"
            },
            "type": "array"
          },
          "upload_id": {
            "description": "UploadId resumes an existing upload instead of creating a new one. The declared files must match the recorded declaration exactly; fresh presigned URLs are returned for all of them. Leave empty to create a new upload.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CreateImportUploadResponse": {
        "properties": {
          "files": {
            "description": "Files to upload, one per declared file, in declaration order.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ImportFileUpload"
            },
            "type": "array"
          },
          "upload_id": {
            "description": "UploadId identifies the upload for resumption and finalization.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CreateIndexGenerationRequest": {
        "properties": {
          "cluster": {
            "description": "Cluster to build the generation on, named as the deployment declares it. Required. A deployment that only has the one cluster declares it as \"default\".",
            "type": "string"
          },
          "name": {
            "description": "Name of the generation. A short readable codename is generated when it is left empty, which is what you want unless the name has to be predictable.",
            "type": "string"
          },
          "prefix": {
            "description": "Prefix is the first segment of every index and alias name the generation owns, and defaults to the generation name. It has to be unique across generations - it is what keeps two generations from writing to each other's indexes - and a lowercase index name segment: it starts with a letter or a digit and continues with letters, digits, underscores or dashes.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CreateIndexGenerationResponse": {
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.IndexGeneration"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CreateSubscriptionRequest": {
        "properties": {
          "keep_alive_seconds": {
            "description": "KeepAliveSeconds makes the subscription expiring: it is deactivated if nothing touches it for this long, and any authenticated RPC against it resets the clock. Clamped to at most seven days; a larger value is clamped, not rejected, and the value that was applied is on the returned subscription.\n Zero, the default, makes it durable: it lives until it is deleted. Expiry is independent of ownership — an organisation-owned subscription can be given a keep-alive, and a subject-owned one can be durable.",
            "type": "integer"
          },
          "owner_tier": {
            "description": "OwnerTier selects who owns the subscription. \"org\", the default, makes it organisation-owned: it belongs to the organisation in the credentials of the caller, and any client of that organisation can manage and poll it. That is the tier for saved searches. \"subject\" makes it owned by the calling subject alone, which is the tier for subscriptions that follow one client session.\n The owner itself is never taken from the request: it is the org claim or the subject of the credentials the call was made with. This only picks which of the two it is.",
            "type": "string"
          },
          "subscription": {
            "$ref": "#/components/schemas/elephant.distribution.SubscriptionSpec"
          }
        },
        "type": "object"
      },
      "elephant.distribution.CreateSubscriptionResponse": {
        "properties": {
          "subscription": {
            "$ref": "#/components/schemas/elephant.distribution.Subscription"
          }
        },
        "type": "object"
      },
      "elephant.distribution.DeleteIndexGenerationRequest": {
        "properties": {
          "name": {
            "description": "Name of the generation to delete.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.DeleteIndexGenerationResponse": {
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.IndexGeneration"
          }
        },
        "type": "object"
      },
      "elephant.distribution.DeleteSubscriptionRequest": {
        "properties": {
          "id": {
            "description": "ID of the subscription to delete. Required. Deleting a subscription that doesn't exist is not an error.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.DeleteSubscriptionResponse": {
        "type": "object"
      },
      "elephant.distribution.DocumentEvent": {
        "description": "DocumentEvent is one entry of the distribution eventlog, which is a transparency log: every entry is hashed over a canonical JSON payload, signed, and chained to its predecessor.\n The signed payload is exactly these nine fields, under these names: id, source, uuid (doc_uuid here), version, type, created_at, published_at, doc_hash and parent. Verifying an entry is: build that object, serialize it as RFC 8785 (JCS) canonical JSON, sha256 it, and check the digest and the ECDSA signature against the key the signature names, fetched from the unauthenticated JWKS at GET /signing-keys. Nothing else on this message is signed - event_type is derived from version, and first_published is a property of the document rather than of the event.\n The verification fields are only populated when the request asked for them; see GetNewDocumentsRequest.verification.",
        "properties": {
          "created_at": {
            "description": "CreatedAt is when distribution emitted the event, in the canonical fixed-width RFC 3339 form used by the transparency log (\"2006-01-02T15:04:05.000000Z\", UTC, microsecond precision) — the same representation that the entry hash is computed over and that archived eventlog chunks carry. It is distribution's own clock, so an event for historical content carries a created_at long after the content was published. Use published_at to date the content.",
            "type": "string"
          },
          "doc_hash": {
            "description": "DocHash is the hex-encoded sha256 of the RFC 8785 canonical form of this version's document data, binding the event to the exact content it announced. Empty for a tombstone.\n Checking it takes two things. It covers the *version* named here, while load_documents delivers the document's current version, so a consumer verifying content fetches the exact version through Content.GetDocumentVersions or Content.GetDocument instead - the two agree only for an event that is still the document's latest. And it covers the stored bytes: rendition links are generated per response and signed for the caller's audience, so ask with disable_renditions or strip the generated rel=\"rendition\" links before hashing.\n Verifying the log itself needs neither - the signature covers this field as a string, whatever content it names. Requires verification.",
            "type": "string"
          },
          "doc_uuid": {
            "description": "DocUUID is the UUID of the affected document.",
            "type": "string"
          },
          "event_type": {
            "description": "EventType is either \"published\" or \"unpublished\".",
            "type": "string"
          },
          "first_published": {
            "description": "FirstPublished is when the document first became public, in the same canonical fixed-width form. The event is about one version, and this is the per-document timestamp beside it: what a consumer groups and dates a document by, as opposed to the version. Equal to published_at for a document's first event, and unchanged by every event after it.\n Not part of the hashed event payload - it is a property of the document rather than of the event, read alongside it.",
            "type": "string"
          },
          "id": {
            "description": "ID of the event.",
            "type": "integer"
          },
          "parent": {
            "description": "Parent is the signature of event id-1, chaining the log, and is empty for event 1. A page therefore verifies as a chain on its own, and a consumer following the log checks that the first entry of a page names the signature of the last entry of the previous one. Requires verification.",
            "type": "string"
          },
          "published_at": {
            "description": "PublishedAt is when the version was published in the source system: the time the \"usable\" status was created in the repository, or the publication time the record carried when the content was bulk imported. Same canonical fixed-width form as created_at, and part of the hashed event payload. This is the publication date of the content, and the timestamp the index anchors and ages documents on.",
            "type": "string"
          },
          "signature": {
            "description": "Signature is the compact signature over the canonical payload, \"v2.{key id}.{base64url sha256}.{base64url ECDSA signature}\". The digest it carries must equal the one recomputed from the payload. Requires verification.",
            "type": "string"
          },
          "source": {
            "description": "Source names the pipeline the event came from: the repository eventlog for content distributed as it is published, or a bulk import job for imported history. Part of the hashed payload.",
            "type": "string"
          },
          "type": {
            "description": "Type of the document, e.g. \"core/article\", as the source event reported it - so a tombstone carries one too. Part of the hashed payload, empty string included: hash what is here, not what a document of that identity looks like now.",
            "type": "string"
          },
          "version": {
            "description": "Version that was published (if this was a publish event).",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.DocumentItem": {
        "properties": {
          "deferred": {
            "description": "Deferred is set when load_documents or subset was requested and this item's document was not loaded because the response had reached its loading bound. Fetch it with Content.GetDocumentVersions.\n An item with neither a document nor deferred set has no document to load: it is a tombstone, or the document has been deleted or erased. Don't ask for it again.",
            "type": "boolean"
          },
          "document": {
            "$ref": "#/components/schemas/newsdoc.Document"
          },
          "event": {
            "$ref": "#/components/schemas/elephant.distribution.DocumentEvent"
          },
          "subset": {
            "description": "Subset is the extracted subset of the document.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ExtractedValues"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.DocumentVersionRef": {
        "description": "DocumentVersionRef identifies one version to load.",
        "properties": {
          "uuid": {
            "description": "UUID of the document.",
            "type": "string"
          },
          "version": {
            "description": "Version to load. Zero is the document's current version, as in GetDocument.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.EraseDocumentRequest": {
        "properties": {
          "uuid": {
            "description": "UUID of the document to erase.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.EraseDocumentResponse": {
        "properties": {
          "status": {
            "$ref": "#/components/schemas/elephant.distribution.ErasureStatus"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ErasureStatus": {
        "description": "ErasureStatus is the state of one document's erasure.",
        "properties": {
          "complete": {
            "description": "Complete is true when the erasure is provably done, i.e. completed_at is set.",
            "type": "boolean"
          },
          "completed_at": {
            "description": "CompletedAt is when the erasure became provable, RFC3339, and empty while it is outstanding. It is set once no archived index snapshot holds a copy of the document any more.",
            "type": "string"
          },
          "pending_partitions": {
            "description": "PendingPartitions are the archived index partitions that still hold a copy. A mounted snapshot is read-only, so the copy in it only goes away when the partition is re-materialized, and the erasure is outstanding until every one of these has been.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "requested_at": {
            "description": "RequestedAt is when the erasure was ordered, RFC3339. A second request for the same document supersedes the first and resets this.",
            "type": "string"
          },
          "uuid": {
            "description": "UUID of the erased document.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ExemplarDocuments": {
        "description": "ExemplarDocuments is a set of documents used as an example of what to match, averaged into a single vector.",
        "properties": {
          "uuids": {
            "description": "UUIDs of the exemplar documents, at least one. A document that isn't indexed, or is of a type indexed without embeddings, is an invalid argument.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ExtractedValue": {
        "properties": {
          "annotation": {
            "description": "Annotations can be used to differentiate between different extracted values.",
            "type": "string"
          },
          "block": {
            "$ref": "#/components/schemas/newsdoc.Block"
          },
          "role": {
            "description": "Role can be used to differentiate between different extracted values.",
            "type": "string"
          },
          "value": {
            "description": "Value is used for extracted attributes and data values.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ExtractedValues": {
        "properties": {
          "extractor": {
            "description": "Extractor index for the the extractor that produced the values.",
            "type": "integer"
          },
          "values": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.ExtractedValue"
            },
            "description": "Values that were extracted.",
            "type": "object"
          }
        },
        "type": "object"
      },
      "elephant.distribution.FacetValues": {
        "description": "FacetValues is the values a facet filter requires. A returned version has to carry all of them, and a request with several facets has to satisfy all of them - the filter is one containment test over the stored facet values. Narrowing to either of two sections is therefore two requests rather than one.",
        "properties": {
          "values": {
            "description": "Values to match. These are document UUIDs, never labels: a section is a core/section document and the facet carries its UUID, so matching is exact and a human-readable name matches nothing at all.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.FinalizeImportUploadRequest": {
        "properties": {
          "expected_documents": {
            "description": "ExpectedDocuments is the total number of documents in the import set, if the client knows it. Optional; when set it is recorded on the import and lets the load cross-check that it saw everything.",
            "type": "integer"
          },
          "source": {
            "description": "Source labels the imported events in the eventlog, so that consumers can tell the import apart from the live repository flow. Required, e.g. \"ttdist\". It cannot be a source the live sync worker writes.",
            "type": "string"
          },
          "upload_id": {
            "description": "UploadId of the upload to finalize.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.FinalizeImportUploadResponse": {
        "properties": {
          "id": {
            "description": "ID of the created import job, for GetImportStatus.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Fuzziness": {
        "description": "Fuzziness is the edit distance allowed when matching terms.",
        "properties": {
          "auto": {
            "$ref": "#/components/schemas/elephant.distribution.FuzzinessAuto"
          },
          "edits": {
            "description": "Edits is the maximum allowed Levenshtein edit distance. Valid values are 0, 1 and 2.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.FuzzinessAuto": {
        "description": "FuzzinessAuto configures automatic fuzziness thresholds. The low and high values are the term length boundaries for the edit distance tiers:\n - Terms shorter than low characters must match exactly (0 edits). - Terms of low to high-1 characters allow at most 1 edit. - Terms of high characters or more allow at most 2 edits.\n Both zero uses the default thresholds, 3 and 6.",
        "properties": {
          "high": {
            "description": "High is the term length where 2 edits start being allowed.",
            "type": "integer"
          },
          "low": {
            "description": "Low is the term length where 1 edit starts being allowed.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetActiveConfigGenerationRequest": {
        "properties": {
          "known_id": {
            "description": "KnownId is the generation ID the client already has; the call returns immediately when the active generation differs. Optional.",
            "type": "integer"
          },
          "only_changed": {
            "description": "OnlyChanged returns an empty response (with Unchanged=true) if the active generation matches KnownId.",
            "type": "boolean"
          },
          "wait_seconds": {
            "description": "WaitSeconds is the maximum number of seconds to wait for the active generation to differ from KnownId. Defaults to 10. Optional.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetActiveConfigGenerationResponse": {
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.ConfigGeneration"
          },
          "unchanged": {
            "description": "Unchanged is true if KnownId matched the active generation and OnlyChanged was set.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetAllActiveSchemasRequest": {
        "properties": {
          "known": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Known schema versions that the client already has. Optional.",
            "type": "object"
          },
          "only_changed": {
            "description": "OnlyChanged only returns the schemas that have been changed.",
            "type": "boolean"
          },
          "wait_seconds": {
            "description": "WaitSeconds is the maximum number of seconds to wait for changes, defaults to 10 seconds. Optional.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetAllActiveSchemasResponse": {
        "properties": {
          "removed": {
            "description": "Removed schemas.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "schemas": {
            "description": "Schemas that currently are active.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Schema"
            },
            "type": "array"
          },
          "unchanged": {
            "description": "Unchanged is set to true if no changes were done compared to known schema versions.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetArchivedDocumentRequest": {
        "properties": {
          "uuid": {
            "description": "UUID of the document.",
            "type": "string"
          },
          "version": {
            "description": "Version to get a URL for.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetArchivedDocumentResponse": {
        "properties": {
          "content_url": {
            "description": "ContentURL is a presigned GET URL for the canonical document JSON.",
            "type": "string"
          },
          "hash": {
            "description": "Hash is the hex-encoded sha256 of the canonical document body.",
            "type": "string"
          },
          "signature": {
            "description": "Signature covers the document body, for end-to-end verification.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetDocumentRequest": {
        "properties": {
          "disable_rendition_signing": {
            "description": "DisableRenditionSigning serves the document with generated but unsigned rendition URLs. See GetNewDocumentsRequest.disable_rendition_signing.",
            "type": "boolean"
          },
          "disable_renditions": {
            "description": "DisableRenditions serves the document without any rendition links. See GetNewDocumentsRequest.disable_renditions.",
            "type": "boolean"
          },
          "renditions": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionParameters"
            },
            "description": "Renditions selects the rendition variants to generate per asset kind. See GetNewDocumentsRequest.renditions.",
            "type": "object"
          },
          "uuid": {
            "description": "UUID of the document to get.",
            "type": "string"
          },
          "version": {
            "description": "Version to get, or 0 for the latest version.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetDocumentResponse": {
        "properties": {
          "document": {
            "$ref": "#/components/schemas/newsdoc.Document"
          },
          "first_published": {
            "description": "FirstPublished is when the document first became public, in the same form. Immutable, and equal to published_at for a document that has only ever been published once. Empty for a document whose first-published record has been erased or was never written.",
            "type": "string"
          },
          "published_at": {
            "description": "PublishedAt is when the returned version became public, in the canonical fixed-width RFC 3339 form (\"2006-01-02T15:04:05.000000Z\", UTC, microsecond precision). For a version served from the archive rather than the hot store this is empty: the archived object carries the document, not the timestamps of the row it was pruned from.",
            "type": "string"
          },
          "version": {
            "description": "Version that was returned.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetDocumentTypesRequest": {
        "type": "object"
      },
      "elephant.distribution.GetDocumentTypesResponse": {
        "properties": {
          "types": {
            "description": "Types that have been defined in the schema.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetDocumentVersionsRequest": {
        "properties": {
          "disable_rendition_signing": {
            "description": "DisableRenditionSigning serves generated but unsigned rendition URLs.",
            "type": "boolean"
          },
          "disable_renditions": {
            "description": "DisableRenditions serves documents without any rendition links.",
            "type": "boolean"
          },
          "load_documents": {
            "description": "LoadDocuments is implied - this RPC exists to load documents - and is only here so that a caller asking for a subset can say it does not also want the whole document. It defaults to true when no subset applies.",
            "type": "boolean"
          },
          "renditions": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionParameters"
            },
            "description": "Renditions selects the rendition variants to generate per asset kind.",
            "type": "object"
          },
          "subset": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.Subsets"
            },
            "description": "Subset returns extracted parts of each document instead of the document, keyed by the document types the expressions apply to.",
            "type": "object"
          },
          "versions": {
            "description": "Versions to load. At least one, and the surplus beyond what one response will load comes back deferred.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.DocumentVersionRef"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetDocumentVersionsResponse": {
        "properties": {
          "versions": {
            "description": "Versions in the order they were requested.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.LoadedDocumentVersion"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetErasureStatusRequest": {
        "properties": {
          "uuid": {
            "description": "UUID of the document to get the erasure status for.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetErasureStatusResponse": {
        "properties": {
          "status": {
            "$ref": "#/components/schemas/elephant.distribution.ErasureStatus"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetImportStatusRequest": {
        "properties": {
          "id": {
            "description": "ID of the import job.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetImportStatusResponse": {
        "properties": {
          "status": {
            "$ref": "#/components/schemas/elephant.distribution.ImportStatus"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetMappingsRequest": {
        "properties": {
          "document_type": {
            "description": "DocumentType to get the mappings of. Required.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetMappingsResponse": {
        "properties": {
          "properties": {
            "description": "Properties of the document type, sorted by name.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.MappingProperty"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetNewDocumentsRequest": {
        "properties": {
          "after": {
            "description": "After returns new documents after the given event ID. A negative value is an offset from the newest event instead: -N returns the last N events (so at head 20, -5 yields events 16 through 20). The count is resolved against the log head at call time and capped at the same page size as a positive read. An offset reaching past the start of the log is clamped to the beginning.",
            "type": "integer"
          },
          "disable_rendition_signing": {
            "description": "DisableRenditionSigning serves documents with generated but unsigned rendition URLs instead of signing the rendition links for the caller's audience. Intended for internal consumers that store canonical content and re-sign for their own audience at their own delivery time; external consumers should leave it unset.",
            "type": "boolean"
          },
          "disable_renditions": {
            "description": "DisableRenditions serves documents without any rendition links: the stored canonical bytes. This is the only mode whose payload matches the stored and archived document, so it is what a doc-hash verification against the transparency log must use. Takes precedence over disable_rendition_signing, which has no effect when this is set.",
            "type": "boolean"
          },
          "load_documents": {
            "description": "LoadDocuments should be set to true if you want the full documents in the response. If subset is used it will override load_documents.",
            "type": "boolean"
          },
          "renditions": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionParameters"
            },
            "description": "Renditions selects the rendition variants to generate per asset kind (e.g. \"image\"). An absent kind - or an empty field within a present entry - falls back to the kind's configured defaults. An entry for a kind the active configuration doesn't declare is an invalid argument.",
            "type": "object"
          },
          "subset": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.Subsets"
            },
            "description": "Subset returns a subset of the documents as specified by a map of newsdoc value extractors keyed by the types that they should be applied to.",
            "type": "object"
          },
          "verification": {
            "description": "Verification populates the transparency-log fields of every event: doc_hash, signature and parent. Together with the fields an event always carries they are the complete signed payload, so a consumer can recompute each entry's hash, verify the signature against the public keys at GET /signing-keys, and check that the page chains. Off by default: the three fields are a few hundred bytes an event that a consumer which doesn't verify would carry for nothing.",
            "type": "boolean"
          },
          "wait_ms": {
            "description": "Wait is the maximum time in milliseconds to wait for new events when the read starts at the head of the log, mirroring the repository eventlog long poll. Defaults to 2000 and is capped at 8000. A negative value disables waiting so that at-head reads return an empty response immediately. Reads that can be answered immediately are always unaffected.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetNewDocumentsResponse": {
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.DocumentItem"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetSchemaRequest": {
        "properties": {
          "name": {
            "description": "Name of the schema to get.",
            "type": "string"
          },
          "version": {
            "description": "Version of the schema to get, optional. The currently active version of the schema will be returned if no version is specified.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetSchemaResponse": {
        "properties": {
          "spec": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetSigningKeysRequest": {
        "type": "object"
      },
      "elephant.distribution.GetSigningKeysResponse": {
        "properties": {
          "keys": {
            "description": "Keys is every published key, not just the one signing right now. A key stops being used for new signatures long before the entries it signed stop needing to be verifiable, so a verifier reading history needs the retired ones too, and picks the key a signature names rather than the newest.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.SigningKey"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetSubscriptionRequest": {
        "properties": {
          "id": {
            "description": "ID of the subscription to get. Required.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetSubscriptionResponse": {
        "properties": {
          "subscription": {
            "$ref": "#/components/schemas/elephant.distribution.Subscription"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetSyncStatusRequest": {
        "type": "object"
      },
      "elephant.distribution.GetSyncStatusResponse": {
        "properties": {
          "caught_up": {
            "description": "CaughtUp is true once the sync worker has replayed the repository eventlog up to the head it had when the replay started. It does not go back to false if the worker later falls behind: it reports that the walk through history is done, not the current lag. Compare Position against the repository eventlog head for that.",
            "type": "boolean"
          },
          "current": {
            "description": "Current is the state the sync worker most recently reported, either \"running\" or \"paused\". Empty if the worker has never reported a state.",
            "type": "string"
          },
          "desired": {
            "description": "Desired is the state the sync worker should be in, either \"running\" or \"paused\".",
            "type": "string"
          },
          "position": {
            "description": "Position is the repository eventlog ID that the sync worker has processed up to.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetTypeConfigurationRequest": {
        "properties": {
          "type": {
            "description": "Type to get configuration for.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.GetTypeConfigurationResponse": {
        "properties": {
          "configuration": {
            "$ref": "#/components/schemas/elephant.distribution.TypeConfiguration"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Hit": {
        "properties": {
          "deferred": {
            "description": "Deferred is set when load_documents or subset was requested and this hit's document was not loaded because the response had reached its loading bound - a hit whose document has aged out of the hot store is an archive read, and a page of them would make the query's response time a function of how old the results are. Fetch it with Content.GetDocumentVersions.\n A hit with neither a document nor deferred set has no document to load: it was deleted or erased since it was indexed.",
            "type": "boolean"
          },
          "document": {
            "$ref": "#/components/schemas/newsdoc.Document"
          },
          "fields": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.StringValues"
            },
            "description": "Fields are the values of the requested fields, keyed by field name. Fields without a value for the document are absent.",
            "type": "object"
          },
          "first_published": {
            "description": "FirstPublished is when the document first became public, in the same form. Immutable, and what a backward-anchored type is partitioned and aged on.",
            "type": "string"
          },
          "id": {
            "description": "ID is the UUID of the document. Pass it to Content.GetDocument to fetch the document itself, or ask the query for it with load_documents.",
            "type": "string"
          },
          "published_at": {
            "description": "PublishedAt is when the indexed version of the document became public, in the canonical fixed-width RFC 3339 form (\"2006-01-02T15:04:05.000000Z\", UTC, microsecond precision).\n It dates the projection that matched, which is not necessarily the document that was loaded: a document corrected since it was indexed is delivered as it stands now, and this is the version the hit was matched on. Empty for a hit out of an index that predates the field.",
            "type": "string"
          },
          "score": {
            "description": "Score is the relevance score of the hit. Only meaningful for hits ordered by relevance; a sorted query leaves it at zero.\n For a knn query the score is the cosine similarity of the document to the query vector, in [-1, 1] and on the same scale as KnnQuery.min_similarity: the server converts it out of OpenSearch's score space so that the two can be compared directly. For a text query it is an unbounded BM25 score, comparable between hits of the same query and meaningless between queries.",
            "type": "number"
          },
          "subset": {
            "description": "Subset is what the document type's extractors collected from the delivered document, when the query named the type in subset.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ExtractedValues"
            },
            "type": "array"
          },
          "version": {
            "description": "Version is the stored version the loaded document or subset was taken from, and zero when neither was requested or the document was no longer there to load.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.HitsTotal": {
        "description": "HitsTotal is the number of matching documents. The count is exact up to a server-side limit, above which it is reported as a lower bound.",
        "properties": {
          "relation": {
            "description": "Relation is \"eq\" when value is the exact number of matches, and \"gte\" when the matches were only counted up to value.",
            "type": "string"
          },
          "value": {
            "description": "Value is the number of matching documents, interpreted according to relation.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ImportFile": {
        "description": "ImportFile declares one file of an import upload.",
        "properties": {
          "name": {
            "description": "Name of the file within the upload, e.g. \"documents-2024-Q2.ndjson.gz\". Names must be unique within the upload. Files are loaded in declaration order.",
            "type": "string"
          },
          "sha256": {
            "description": "Sha256 is the hex-encoded sha256 of the file contents, verified at finalization.",
            "type": "string"
          },
          "size": {
            "description": "Size of the file in bytes.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ImportFileUpload": {
        "description": "ImportFileUpload is the upload destination for one declared file.",
        "properties": {
          "expires_at": {
            "description": "ExpiresAt is when the URL stops working, RFC3339. Resume the upload to get fresh URLs.",
            "type": "string"
          },
          "name": {
            "description": "Name of the file, as declared.",
            "type": "string"
          },
          "upload_url": {
            "description": "UploadUrl is a presigned PUT URL for the file contents. Object keys and the presigning mechanism are internal - the URL is used as-is and never parsed.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ImportStatus": {
        "description": "ImportStatus is the recorded state of one import job.",
        "properties": {
          "created_at": {
            "description": "CreatedAt is when the import was finalized, RFC3339.",
            "type": "string"
          },
          "current_file": {
            "description": "CurrentFile is the declared file the load is working through, and the last one once the import is done.",
            "type": "string"
          },
          "documents_loaded": {
            "description": "DocumentsLoaded is how many documents have been stored so far.",
            "type": "integer"
          },
          "error": {
            "description": "Error is what a failed import failed with, empty otherwise.",
            "type": "string"
          },
          "expected_documents": {
            "description": "ExpectedDocuments is the total declared at finalization, or zero if none was.",
            "type": "integer"
          },
          "id": {
            "description": "ID of the import job.",
            "type": "string"
          },
          "quarter_high_water_mark": {
            "description": "QuarterHighWaterMark is the most recent quarter the import has fully loaded, e.g. \"2024q2\", empty until the first quarter completes. Quarters are loaded in order, so overlay-pressure re-materializations of quarters the mark has not passed are held until it does - one rebuild per touched quarter, after the import has moved on. Deadline-driven re-materializations (tombstone, erasure) are never held.",
            "type": "string"
          },
          "source": {
            "description": "Source the imported events carry in the eventlog.",
            "type": "string"
          },
          "status": {
            "description": "Status is \"pending\" until the load starts, \"loading\" while it runs, and \"done\", \"failed\" or \"cancelled\" when it ends. A failed import is resumed by the service, not re-created: documents are idempotent per version, so the load continues from its recorded progress. Only CancelImport moves a job to \"cancelled\", and only from there does the loader move on to the jobs queued behind it.",
            "type": "string"
          },
          "updated_at": {
            "description": "UpdatedAt is when the recorded state last changed, RFC3339.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.IndexGeneration": {
        "description": "IndexGeneration is one complete set of search indexes: everything under one index name prefix, on one cluster, with an eventlog cursor of its own.",
        "properties": {
          "activated_at": {
            "description": "ActivatedAt is when it was last activated, RFC3339, and empty for a generation that has never served.",
            "type": "string"
          },
          "active": {
            "description": "Active is whether this is the generation that search, calibration and subscription matching use.",
            "type": "boolean"
          },
          "cluster": {
            "description": "Cluster the generation's indexes live on, named as the deployment declares it. Clusters are configuration, so their URLs and credentials are never part of a response.",
            "type": "string"
          },
          "created_at": {
            "description": "CreatedAt is when the generation was registered, RFC3339.",
            "type": "string"
          },
          "enabled": {
            "description": "Enabled is whether an indexer is run for the generation. A disabled generation keeps its indexes but stops consuming the eventlog, and falls behind for as long as it stays that way.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the generation. It identifies the generation in these RPCs, in the generation override on Search.Query, and in metric labels.",
            "type": "string"
          },
          "prefix": {
            "description": "Prefix is the first segment of every index and alias name the generation owns, and what scopes it: no two generations share one.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.IndexGenerationStatus": {
        "description": "IndexGenerationStatus is a generation together with how far it has got.",
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.IndexGeneration"
          },
          "lag": {
            "description": "Lag is how far the generation is behind the head of the eventlog, which is what the activation gate is checked against.",
            "type": "integer"
          },
          "position": {
            "description": "Position is the eventlog ID the generation's indexer has processed up to, and zero for a generation whose indexer has not stored a position yet - either because it has not started or because it is still draining the archive.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.KnnQuery": {
        "description": "KnnQuery is a semantic query: it looks up the documents whose embeddings are closest to a query vector, rather than the documents that contain the query terms.\n The clause has two independent choices, and the server validates both - the fields are plain fields rather than oneofs so that the combinations can be relaxed later without renumbering, but an invalid combination is rejected as an invalid argument, it is never guessed at.\n 1. Where the query vector comes from: exactly one of text and vector. 2. How many neighbours come back: exactly one of k and min_similarity.\n Similarity is cosine similarity throughout, in the range [-1, 1], where higher means closer and 1 means identical direction. The embeddings are normalized, so this is the same dial as maximum inner product; the server converts from OpenSearch's own score space so that both min_similarity and the per-hit scores are stated as cosine similarity. Hit.score for a knn hit is therefore directly comparable to min_similarity, which is how a threshold is found: run a k query, look at where the results stop being relevant, and read the threshold off the scores.\n Thresholds are model-dependent and are not portable between deployments or across an embedding model upgrade. Treat a min_similarity value as something to recalibrate, not as a constant.",
        "properties": {
          "k": {
            "description": "K is the number of nearest neighbours to retrieve. The hits come back ordered by similarity, and there are always k of them (fewer only when the filtered candidate set is smaller), no matter how poor the last ones are - a k query has no relevance floor. Mutually exclusive with min_similarity, and one of the two must be set.",
            "type": "integer"
          },
          "min_similarity": {
            "description": "MinSimilarity turns the clause into a radial query: every document at or above this cosine similarity is a hit, however many that turns out to be, up to size. Use it when \"everything that is at least this close\" is the question - it makes a result set that does not silently pad itself with weak matches the way a fixed k does, and it is the same predicate that subscriptions match on, so a query is how a subscription threshold is previewed.\n Cosine similarity in [-1, 1]; higher is closer. Mutually exclusive with k, and one of the two must be set. The field is optional so that a floor of 0 - every document that isn't pointing away from the query - can be asked for, and is distinct from not asking for a radial query at all.",
            "type": "number"
          },
          "text": {
            "description": "Text to search for, embedded server-side with the current embedding model. This is the normal way to run a semantic query, and the only one that survives a model upgrade unchanged, since the server re-embeds the text with whatever model is current.",
            "type": "string"
          },
          "vector": {
            "description": "Vector to search for, as a raw embedding. Advanced use: the caller has to produce it with the same model and dimension as the index, and a model upgrade invalidates it. Prefer text unless the vector comes from somewhere the server cannot re-derive it from. A vector of the wrong dimension is an invalid argument.",
            "items": {
              "type": "number"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.LabelExpression": {
        "properties": {
          "expression": {
            "description": "Expression is a newsdoc value extraction expression.",
            "type": "string"
          },
          "template": {
            "description": "Template is the template that turns the extracted values into a label.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListArchiveChunksRequest": {
        "properties": {
          "after_chunk_id": {
            "description": "AfterChunkID lists chunks after the given chunk ID; 0 to start.",
            "type": "integer"
          },
          "page_size": {
            "description": "PageSize defaults to 100.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListArchiveChunksResponse": {
        "properties": {
          "chunks": {
            "description": "Chunks in this page, in ascending chunk-ID order.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ArchiveChunk"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListArchivedDocumentsRequest": {
        "properties": {
          "cursor": {
            "description": "Cursor from a previous response; empty to start a new drain.",
            "type": "string"
          },
          "page_size": {
            "description": "PageSize defaults to 1000.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListArchivedDocumentsResponse": {
        "properties": {
          "documents": {
            "description": "Documents in this page.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ArchivedDocument"
            },
            "type": "array"
          },
          "next_cursor": {
            "description": "NextCursor is empty when the catalog has been exhausted.",
            "type": "string"
          },
          "watermark_event_id": {
            "description": "WatermarkEventID is the last archived event ID as of the start of the drain. A consumer that has drained the catalog should tail Content.GetNewDocuments from this ID: everything up to it is covered by the catalog, everything after it by the tail.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListConfigGenerationsRequest": {
        "properties": {
          "before": {
            "description": "Before is the highest generation ID to return; defaults to the latest. Use the smallest returned ID to paginate further into the past.",
            "type": "integer"
          },
          "page_size": {
            "description": "PageSize defaults to 50, max 200.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListConfigGenerationsResponse": {
        "properties": {
          "generations": {
            "description": "Generations without schemas or type configurations populated.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ConfigGeneration"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListImportsRequest": {
        "type": "object"
      },
      "elephant.distribution.ListImportsResponse": {
        "properties": {
          "imports": {
            "description": "Imports, most recent first.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ImportStatus"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListIndexGenerationsRequest": {
        "type": "object"
      },
      "elephant.distribution.ListIndexGenerationsResponse": {
        "properties": {
          "eventlog_head": {
            "description": "EventlogHead is the ID of the last event in the distribution eventlog, the position a generation that has caught up converges on.",
            "type": "integer"
          },
          "generations": {
            "description": "Generations, oldest first.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.IndexGenerationStatus"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListPlannedVersionsRequest": {
        "properties": {
          "after": {
            "description": "After is a cursor from a previous response. Empty starts at the earliest end of the interval.\n Unlike the published day, this cursor sits on a mutable key: a rescheduled item moves, so it can cross a page boundary while a client pages an interval, and appear twice or not at all. Bounded and deliberate for a calendar - do not read exactness into it.",
            "type": "string"
          },
          "disable_rendition_signing": {
            "description": "DisableRenditionSigning serves documents with generated but unsigned rendition URLs.",
            "type": "boolean"
          },
          "disable_renditions": {
            "description": "DisableRenditions serves documents without any rendition links.",
            "type": "boolean"
          },
          "document_types": {
            "description": "DocumentTypes to include. At least one.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "facets": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.FacetValues"
            },
            "description": "Facets narrows the result to documents carrying the given values. See ListPublishedVersionsRequest.facets.",
            "type": "object"
          },
          "from": {
            "description": "From is the start of the interval on the planned date, inclusive, as an RFC 3339 timestamp with an offset. Required. See ListPublishedVersionsRequest.from for why this is an interval and not a day.",
            "type": "string"
          },
          "limit": {
            "description": "Limit is the maximum number of entries to return. Defaults to 50 and is capped at 200.",
            "type": "integer"
          },
          "load_documents": {
            "description": "LoadDocuments delivers the document with each entry.",
            "type": "boolean"
          },
          "renditions": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionParameters"
            },
            "description": "Renditions selects the rendition variants to generate per asset kind.",
            "type": "object"
          },
          "subset": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.Subsets"
            },
            "description": "Subset returns a subset of each document. Overrides load_documents for the types it names.",
            "type": "object"
          },
          "to": {
            "description": "To is the end of the interval, exclusive, as an RFC 3339 timestamp with an offset. Required.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListPlannedVersionsResponse": {
        "properties": {
          "inline_horizon": {
            "description": "InlineHorizon is the oldest instant whose documents still come back with the response. See ListPublishedVersionsResponse.inline_horizon.",
            "type": "string"
          },
          "items": {
            "description": "Items are what is planned for the interval, earliest first. Current versions only, so superseded_at is always empty.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.PublishedVersion"
            },
            "type": "array"
          },
          "next": {
            "description": "Next is the cursor to pass as after for the next page, and empty at the end of the interval.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListPublishedVersionsRequest": {
        "properties": {
          "after": {
            "description": "After is a cursor from a previous response. Empty starts at the newest end of the interval. Cursors are opaque and stay valid indefinitely: the sort key of an entry is its publication time, which never changes.",
            "type": "string"
          },
          "disable_rendition_signing": {
            "description": "DisableRenditionSigning serves documents with generated but unsigned rendition URLs. See GetNewDocumentsRequest.disable_rendition_signing.",
            "type": "boolean"
          },
          "disable_renditions": {
            "description": "DisableRenditions serves documents without any rendition links. See GetNewDocumentsRequest.disable_renditions.",
            "type": "boolean"
          },
          "document_types": {
            "description": "DocumentTypes to include. At least one; several at a time is the normal shape, as a newsroom flow lists flashes, articles and editorial information together.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "facets": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.FacetValues"
            },
            "description": "Facets narrows the result to versions carrying the given values, keyed by facet name. A version has to carry every named facet and every value within it. The extraction is per type and per version, so a version that carried a section when it was published matches even if a later version doesn't - which is right for a publication log: it was published in that section at that minute.",
            "type": "object"
          },
          "from": {
            "description": "From is the start of the interval, inclusive, as an RFC 3339 timestamp with an offset. Required.\n The server has no notion of a day: what a day is, in which timezone, and whether the boundary falls at midnight or at the start of a shift is the client's decision. The interval is normalised to UTC for the scan, so an offset is how a local day is expressed.",
            "type": "string"
          },
          "limit": {
            "description": "Limit is the maximum number of entries to return. Defaults to 50 and is capped at 200.",
            "type": "integer"
          },
          "load_documents": {
            "description": "LoadDocuments delivers the document with each entry. Unlike Search.Query, which loads the current version, this loads the version the entry is.",
            "type": "boolean"
          },
          "renditions": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionParameters"
            },
            "description": "Renditions selects the rendition variants to generate per asset kind. See GetNewDocumentsRequest.renditions.",
            "type": "object"
          },
          "subset": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.Subsets"
            },
            "description": "Subset returns a subset of each document as specified by a map of newsdoc value extractors keyed by the types they apply to. Overrides load_documents for the types it names.",
            "type": "object"
          },
          "to": {
            "description": "To is the end of the interval, exclusive, as an RFC 3339 timestamp with an offset. Required. The span is capped by the server; a wider interval is an invalid argument.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListPublishedVersionsResponse": {
        "properties": {
          "inline_horizon": {
            "description": "InlineHorizon is the oldest instant whose documents still come back with the response, in the canonical fixed-width RFC 3339 form.\n It is not a limit on what the view can answer. Retention drops document bodies and keeps their bookkeeping, so any interval can be listed; what changes past this point is that every document has to be streamed in with GetDocumentVersions rather than delivered inline. A client uses it to decide how hard to prefetch.",
            "type": "string"
          },
          "items": {
            "description": "Items are the publication events of the interval, newest first.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.PublishedVersion"
            },
            "type": "array"
          },
          "next": {
            "description": "Next is the cursor to pass as after for the next page, and empty at the end of the interval.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListSubscriptionsRequest": {
        "properties": {
          "include_deactivated": {
            "description": "IncludeDeactivated includes subscriptions that have expired and are waiting to be reaped. They are left out by default.",
            "type": "boolean"
          },
          "owner_tier": {
            "description": "OwnerTier filters on ownership: \"org\" for the subscriptions of the organisation of the caller, \"subject\" for those owned by the calling subject. Empty lists both.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ListSubscriptionsResponse": {
        "properties": {
          "subscriptions": {
            "description": "Subscriptions the caller can see, most recently created first.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Subscription"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.LoadedDocumentVersion": {
        "properties": {
          "deferred": {
            "description": "Deferred is set when this version was beyond the response's loading bound. Ask again - a client that re-requests what it did not get makes progress.",
            "type": "boolean"
          },
          "document": {
            "$ref": "#/components/schemas/newsdoc.Document"
          },
          "first_published": {
            "description": "FirstPublished is when the document first became public, same form.",
            "type": "string"
          },
          "found": {
            "description": "Found is false when the version is in neither the store nor the archive: erased, deleted, or never distributed. Don't ask again.",
            "type": "boolean"
          },
          "published_at": {
            "description": "PublishedAt is when this version became public, in the canonical fixed-width RFC 3339 form. Empty for a version served from the archive.",
            "type": "string"
          },
          "subset": {
            "description": "Subset is what the type's extractors collected from it.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ExtractedValues"
            },
            "type": "array"
          },
          "uuid": {
            "description": "UUID of the document.",
            "type": "string"
          },
          "version": {
            "description": "Version that was loaded. For a request that asked for version zero this is the version the document is currently at.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MappingField": {
        "description": "MappingField is an alternative indexing of a property.",
        "properties": {
          "name": {
            "description": "Name of the field, appended to the property name with a dot to query it.",
            "type": "string"
          },
          "type": {
            "description": "Type of the field, from the same vocabulary as the property type, plus \"prefix\" for fields analysed for prefix matching.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MappingProperty": {
        "description": "MappingProperty is a searchable field of a document type.",
        "properties": {
          "fields": {
            "description": "Fields are alternative indexings of the property, queried as \"name.field\". A text property commonly has a \"keyword\" field for exact matching and sorting, and a \"prefix\" field for as-you-type matching.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.MappingField"
            },
            "type": "array"
          },
          "name": {
            "description": "Name of the property, as used in queries.",
            "type": "string"
          },
          "path": {
            "description": "Path is the property that an alias property points to. Empty for everything but aliases.",
            "type": "string"
          },
          "type": {
            "description": "Type of the property: \"text\", \"keyword\", \"date\", \"long\", \"double\", \"boolean\", or \"alias\" for a property that is another name for the property named by path.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MatchAllQuery": {
        "description": "MatchAllQuery matches every document. Useful as the base of a query that only filters, and for browsing a type by sort order.",
        "type": "object"
      },
      "elephant.distribution.MatchPhraseQuery": {
        "description": "MatchPhraseQuery matches analysed text where the terms occur in the given order, next to each other.",
        "properties": {
          "field": {
            "description": "Field to match against.",
            "type": "string"
          },
          "value": {
            "description": "Value to match as a phrase.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MatchProvenance": {
        "description": "MatchProvenance records what fired, so that a match can be explained and so that feedback about it can be routed to the part of the subscription that caused it.",
        "properties": {
          "clauses": {
            "description": "Clauses are the vector clauses of the subscription, in the order they are stored in, with the similarities they scored.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ClauseProvenance"
            },
            "type": "array"
          },
          "query_matched": {
            "description": "QueryMatched is whether the query part of the subscription matched. False when the subscription has no query part.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MatchQuery": {
        "description": "MatchQuery matches analysed text: the value is run through the analyser of the field, and a document matches if any of the resulting terms do.",
        "properties": {
          "boost": {
            "description": "Boost multiplies the score contribution of the clause. Defaults to 1.",
            "type": "number"
          },
          "field": {
            "description": "Field to match against.",
            "type": "string"
          },
          "value": {
            "description": "Value to match.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MemberProvenance": {
        "description": "MemberProvenance is the similarity one clause member scored against the document.",
        "properties": {
          "member_index": {
            "description": "MemberIndex is the position of the member in VectorClause.members.",
            "type": "integer"
          },
          "passed": {
            "description": "Passed is whether the similarity reached the threshold of the member. In a negated clause a passing member is what would have excluded the document, so all of them are false on a match.",
            "type": "boolean"
          },
          "similarity": {
            "description": "Similarity is the highest cosine similarity between the vector of the member and the chunk embeddings of the document, on the same scale as VectorClauseMember.min_similarity.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "elephant.distribution.MultiMatchQuery": {
        "description": "MultiMatchQuery matches analysed text against several fields at once.",
        "properties": {
          "boolean_and": {
            "description": "BooleanAnd requires all terms of the query to match.",
            "type": "boolean"
          },
          "boost": {
            "description": "Boost multiplies the score contribution of the clause. Defaults to 1.",
            "type": "number"
          },
          "fields": {
            "description": "Fields to match against. A field can carry a boost as a \"^\"-suffix, e.g. \"document.title^3\".",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "fuzziness": {
            "$ref": "#/components/schemas/elephant.distribution.Fuzziness"
          },
          "minimum_should_match": {
            "description": "MinimumShouldMatch is the number of query terms that must match when the terms are combined with \"or\". Takes the full minimum-should-match expression syntax, e.g. \"2\" or \"75%\".",
            "type": "string"
          },
          "prefix_length": {
            "description": "PrefixLength is the number of leading characters that fuzzy matching leaves unchanged. Defaults to 0.",
            "type": "integer"
          },
          "query": {
            "description": "Query text to match.",
            "type": "string"
          },
          "tie_breaker": {
            "description": "TieBreaker is how much the score of matches in other fields than the best-scoring one contributes, between 0 and 1. Defaults to 0, i.e. only the best field counts.",
            "type": "number"
          },
          "type": {
            "description": "Type is the multi-match query type. Valid values are best_fields, most_fields, cross_fields, phrase, phrase_prefix and bool_prefix. Defaults to best_fields.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PollMatchesRequest": {
        "properties": {
          "after_event_id": {
            "description": "AfterEventID returns matches with a higher event ID than this one. Start at 0 to read the log from the beginning, and pass the event ID of the last match you processed on every call after that.",
            "type": "integer"
          },
          "subscription_id": {
            "description": "SubscriptionID to read matches for. Required.",
            "type": "string"
          },
          "wait_ms": {
            "description": "WaitMS is the maximum time in milliseconds to wait for a match when there is nothing to return yet. Defaults to 2000 and is capped by the server. A negative value disables waiting, so a caller that is draining a backlog returns immediately at the end of it. A call that can be answered from the log doesn't wait.\n Waiting is not a guarantee of getting the whole batch: the server waits for the first match, gives the rest of the batch a moment to land, and returns what is there. Keep polling.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PollMatchesResponse": {
        "properties": {
          "matches": {
            "description": "Matches in ascending event ID order. Empty when the wait ran out without anything matching, which is the normal idle response.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.SubscriptionMatch"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PrefixQuery": {
        "description": "PrefixQuery matches values that start with the given value. Run it against a keyword field, or against a field that the mappings report as \"prefix\": those are analysed for prefix matching and are much cheaper to search as you type.",
        "properties": {
          "boost": {
            "description": "Boost multiplies the score contribution of the clause. Defaults to 1.",
            "type": "number"
          },
          "case_insensitive": {
            "description": "CaseInsensitive matches regardless of case.",
            "type": "boolean"
          },
          "field": {
            "description": "Field to match against.",
            "type": "string"
          },
          "value": {
            "description": "Value that the field must start with.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PreviewMatch": {
        "description": "PreviewMatch is a document that a previewed subscription would have matched. It is the match log entry as it would have looked, minus the event: a preview runs against the indexed documents of the window, not against the events that produced them.",
        "properties": {
          "fields": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.StringValues"
            },
            "description": "Fields are the values the request's field selection asked for. The title is not among them unless the selection named it too.",
            "type": "object"
          },
          "first_published": {
            "description": "FirstPublished is when the document was first published, RFC3339. This is what max_age_seconds is evaluated against.",
            "type": "string"
          },
          "provenance": {
            "$ref": "#/components/schemas/elephant.distribution.MatchProvenance"
          },
          "title": {
            "description": "Title of the document, always returned. A dry run is read to decide whether the matches are the right ones, and that judgement is made on what the documents are, not on their identifiers.\n Empty for a document the index holds no title for.",
            "type": "string"
          },
          "type": {
            "description": "Type of the document.",
            "type": "string"
          },
          "uuid": {
            "description": "UUID of the document. Pass it to Content.GetDocument to read it.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PreviewSubscriptionRequest": {
        "properties": {
          "fields": {
            "description": "Fields to return with each match, named as flat index fields (e.g. \"document.rel.section.uuid\"), as in Search.Query. The title is returned whether or not it is selected.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "size": {
            "description": "Size is the maximum number of matches to return, most recent first. Defaults to 50 and is capped by the server. The total is reported separately, so a preview that returns the first page still says how much the subscription would have fired.",
            "type": "integer"
          },
          "subscription": {
            "$ref": "#/components/schemas/elephant.distribution.SubscriptionSpec"
          },
          "window_days": {
            "description": "WindowDays is the number of days back to evaluate. Defaults to 7 and is capped by what the recent tier holds.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PreviewSubscriptionResponse": {
        "properties": {
          "matches": {
            "description": "Matches in the window, most recently published first.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.PreviewMatch"
            },
            "type": "array"
          },
          "matches_per_day": {
            "description": "MatchesPerDay is total spread over the window, the rate the subscription would have fired at. A subscription that fires more often than the surface it feeds can carry needs a tighter threshold, not a filter on the consuming side.",
            "type": "number"
          },
          "total": {
            "$ref": "#/components/schemas/elephant.distribution.HitsTotal"
          },
          "window_end": {
            "description": "WindowEnd is the end of the evaluated window, RFC3339.",
            "type": "string"
          },
          "window_start": {
            "description": "WindowStart is the beginning of the evaluated window, RFC3339.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.PublishedVersion": {
        "description": "PublishedVersion is one stored document version in a daily view.",
        "properties": {
          "anchor": {
            "description": "Anchor is the date the document is planned for, in the same form: the resolved partition anchor of a forward-anchored type. Empty for types that carry no dates of their own. This is what ListPlannedVersions orders and filters on.",
            "type": "string"
          },
          "deferred": {
            "description": "Deferred is set when load_documents or subset was requested and this entry's document was not loaded because the response had reached its loading bound. Fetch it with Content.GetDocumentVersions, passing doc_uuid and version.\n An entry with neither a document nor deferred set has no document to load - it has been erased - so don't ask for it again.",
            "type": "boolean"
          },
          "doc_uuid": {
            "description": "DocUUID is the UUID of the document.",
            "type": "string"
          },
          "document": {
            "$ref": "#/components/schemas/newsdoc.Document"
          },
          "first_published": {
            "description": "FirstPublished is when the document first became public, in the same form. Equal to published_at for a version that is the document's first.",
            "type": "string"
          },
          "published_at": {
            "description": "PublishedAt is when this version became public, in the canonical fixed-width RFC 3339 form (\"2006-01-02T15:04:05.000000Z\", UTC, microsecond precision). This is what ListPublishedVersions orders and filters on.",
            "type": "string"
          },
          "subset": {
            "description": "Subset is what the document type's extractors collected from the delivered document, when the request named the type in subset.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ExtractedValues"
            },
            "type": "array"
          },
          "superseded_at": {
            "description": "SupersededAt is when a later version of the document was published, in the same form, and empty while this is the current version. A published-day view renders superseded entries as history: they are what was published at that minute, not what stands now.\n Always empty in ListPlannedVersions, which only returns current versions.",
            "type": "string"
          },
          "type": {
            "description": "Type of the document.",
            "type": "string"
          },
          "version": {
            "description": "Version of the document this entry is, in the same numbering as DocumentEvent.version and Content.GetDocument.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Query": {
        "description": "Query is a single query clause. Exactly one of the conditions must be set; an empty Query is an invalid argument.",
        "properties": {
          "bool": {
            "$ref": "#/components/schemas/elephant.distribution.BoolQuery"
          },
          "exists": {
            "description": "Exists matches documents that have a value for the named field.",
            "type": "string"
          },
          "match": {
            "$ref": "#/components/schemas/elephant.distribution.MatchQuery"
          },
          "match_all": {
            "$ref": "#/components/schemas/elephant.distribution.MatchAllQuery"
          },
          "match_phrase": {
            "$ref": "#/components/schemas/elephant.distribution.MatchPhraseQuery"
          },
          "multi_match": {
            "$ref": "#/components/schemas/elephant.distribution.MultiMatchQuery"
          },
          "prefix": {
            "$ref": "#/components/schemas/elephant.distribution.PrefixQuery"
          },
          "query_string": {
            "description": "QueryString is a query in the query string mini-language. Use it for user-entered search expressions; prefer the structured clauses for queries the client builds itself.",
            "type": "string"
          },
          "range": {
            "$ref": "#/components/schemas/elephant.distribution.RangeQuery"
          },
          "term": {
            "$ref": "#/components/schemas/elephant.distribution.TermQuery"
          },
          "terms": {
            "$ref": "#/components/schemas/elephant.distribution.TermsQuery"
          }
        },
        "type": "object"
      },
      "elephant.distribution.QueryRequest": {
        "properties": {
          "date_from": {
            "description": "DateFrom and DateTo restrict the result to documents whose anchor falls in the interval, as RFC 3339 timestamps. The interval is half-open - from is inclusive, to is exclusive - so consecutive windows tile without returning a document on the boundary twice. Either bound may be left empty for an interval open on that side.\n The anchor is whatever the document type is partitioned on, not a named field: first_published for a type dated by publication, and the time expression the document was routed on for one dated by what it describes - a planning item covering next week is in next week's window, whenever it was written. So the same request means \"in this period\" for every type, and a caller does not have to know which field a type is anchored on. Types that aren't partitioned by time, the entity registries such as sections and authors, have no anchor and are returned regardless, exactly as the tier window leaves them alone.\n This is the way to scope a semantic query to a period, and the only way: a range clause cannot be combined with knn - see query - so a date range written that way is an invalid argument rather than an answer that cannot be trusted. The interval is applied around the k-NN clause rather than inside it, so it also narrows the query to the partitions the interval intersects, and the neighbours are drawn from those partitions rather than from the whole tier. Within them the interval is applied to the neighbours after they are picked, so a k-bounded query whose interval covers part of a partition can return fewer than k hits; a radial query bounded by min_similarity is unaffected, which is what to reach for when the count matters.",
            "type": "string"
          },
          "date_to": {
            "type": "string"
          },
          "disable_rendition_signing": {
            "description": "DisableRenditionSigning serves the documents with generated but unsigned rendition URLs. See GetNewDocumentsRequest.disable_rendition_signing.",
            "type": "boolean"
          },
          "disable_renditions": {
            "description": "DisableRenditions serves the documents without any rendition links. See GetNewDocumentsRequest.disable_renditions.",
            "type": "boolean"
          },
          "document_types": {
            "description": "DocumentTypes to search. Empty searches all types the caller can read. A type that isn't indexed is an invalid argument.\n For a semantic query (knn) the empty list is every indexed type that carries embeddings: the types that don't are skipped rather than refused, since a deployment normally indexes entity registries that have no vectors beside the temporal types that do. A type that is named explicitly is still an invalid argument when it has no embeddings - it is a question that cannot be answered, and an empty result would look like one.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "fields": {
            "description": "Fields to return with each hit, named as flat index fields (e.g. \"document.title\"). Empty returns hits without field values, which is the cheapest form when the caller only needs the UUIDs.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "from": {
            "description": "From is the number of hits to skip, for paging through a result set. Deep paging is bounded by the server; use a narrower query or a sort on a stable field instead of paging far into a large result.",
            "type": "integer"
          },
          "generation": {
            "description": "Generation answers the query from a named index generation instead of the active one, naming it as Configuration.ListIndexGenerations does. Empty is the active generation, and the only value an ordinary caller may send: naming a generation requires the \"dist_admin\" scope.\n It is the validation surface for a rebuild or a move to another cluster. A candidate generation is built and kept up to date while the active one serves, and this is how real queries are pointed at it and the results compared before it is activated. A generation that doesn't exist, or one whose cluster is unreachable, is an error rather than a silent fall back to the active generation - a comparison that quietly queried the wrong index set would be worse than no comparison.",
            "type": "string"
          },
          "knn": {
            "$ref": "#/components/schemas/elephant.distribution.KnnQuery"
          },
          "language": {
            "description": "Language to search, as the language tag the documents are indexed under, e.g. \"sv\" or \"sv-se\". Required: a query targets exactly one language series so that fan-out stays explicit. An unknown language is an invalid argument.",
            "type": "string"
          },
          "load_documents": {
            "description": "LoadDocuments returns the document itself with each hit, as Content.GetDocument would serve it: the current stored version, with the rendition links generated and signed for the caller. It requires the \"dist_read\" scope on top of the search scope - \"dist_search\" alone grants queries over the index, not the content behind it.\n A page of hits is what bounds it - the server's page size cap applies to a query that loads documents as it does to any other. A hit whose document is no longer stored is returned without one rather than failing the query.",
            "type": "boolean"
          },
          "query": {
            "$ref": "#/components/schemas/elephant.distribution.Query"
          },
          "renditions": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionParameters"
            },
            "description": "Renditions selects the rendition variants to generate per asset kind. See GetNewDocumentsRequest.renditions.",
            "type": "object"
          },
          "size": {
            "description": "Size is the maximum number of hits to return. Defaults to 50, and is capped by the server.",
            "type": "integer"
          },
          "sort": {
            "description": "Sort order of the hits. Empty sorts by relevance, descending.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Sort"
            },
            "type": "array"
          },
          "subset": {
            "additionalProperties": {
              "$ref": "#/components/schemas/elephant.distribution.Subsets"
            },
            "description": "Subset returns extracted parts of the documents instead of the whole of them, as a map of newsdoc value extractors keyed by the document types they apply to. It overrides load_documents for the types it names, and requires the same \"dist_read\" scope.\n Extraction runs on the delivered document, so an extracted image block carries the same generated rendition links the whole document would have been served with.",
            "type": "object"
          },
          "tier": {
            "description": "Tier to search: \"recent\" for the rolling recent window, \"all\" for the recent window plus the archive. Empty defaults to \"recent\". Any other value is an invalid argument.\n \"recent\" is not \"whatever the cluster happens to hold locally\". It applies an implicit window on the document's anchor timestamp, so that recent means the same thing on the first day of a quarter as on the last. For document types anchored on when they happened - news, and everything else dated by publication - that window is a first_published no older than the configured recent window, 30 days by default. Types that aren't partitioned by time, the entity registries such as sections and authors, are never windowed and are returned in full by both tiers.\n \"all\" searches the archive set as well. The archive is served from snapshots mounted on separate nodes, so it answers more slowly, and the higher latency is accepted rather than worked around - which is why it is an explicit choice and not the default. Scope an archive query with a range on the anchor field whenever you can: given a range, the server resolves the query to the archived partitions that intersect it instead of fanning out over every quarter ever indexed.\n A document that has been corrected after its partition was archived exists in both tiers. Hits are collapsed on the document UUID, and the field values reported are those of the highest indexed version, so a cross-tier query never returns the same document twice and never reports the superseded copy's values.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.QueryResponse": {
        "properties": {
          "hits": {
            "description": "Hits in the requested order, at most size of them.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.Hit"
            },
            "type": "array"
          },
          "total": {
            "$ref": "#/components/schemas/elephant.distribution.HitsTotal"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RangeQuery": {
        "description": "RangeQuery matches a field against an interval. The bounds are written as strings and parsed according to the type of the field, so a date field takes RFC 3339 timestamps and a numeric field takes numbers. At least one bound must be set; combining a lower and an upper bound gives a closed interval.",
        "properties": {
          "field": {
            "description": "Field to match against.",
            "type": "string"
          },
          "gt": {
            "description": "Gt is an exclusive lower bound.",
            "type": "string"
          },
          "gte": {
            "description": "Gte is an inclusive lower bound.",
            "type": "string"
          },
          "lt": {
            "description": "Lt is an exclusive upper bound.",
            "type": "string"
          },
          "lte": {
            "description": "Lte is an inclusive upper bound.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RawVector": {
        "description": "RawVector is an embedding supplied by the caller.",
        "properties": {
          "values": {
            "description": "Values of the embedding, which must have the dimension of the index.",
            "items": {
              "type": "number"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RegisterConfigGenerationRequest": {
        "properties": {
          "activate": {
            "description": "Activate the new generation immediately. Defaults to false; callers typically register first and activate as a separate step so they can inspect the diff first.",
            "type": "boolean"
          },
          "description": {
            "description": "Description is an optional human-readable tag for the new generation.",
            "type": "string"
          },
          "renditions": {
            "description": "Renditions the generation should contain, one per asset kind. Order of the sources within each kind is preserved.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionConfiguration"
            },
            "type": "array"
          },
          "schemas": {
            "description": "Schemas the generation should contain. Order is preserved.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ConfigGenerationSchema"
            },
            "type": "array"
          },
          "types": {
            "description": "Types the generation should contain. Order is preserved.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.ConfigGenerationType"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RegisterConfigGenerationResponse": {
        "properties": {
          "generation": {
            "$ref": "#/components/schemas/elephant.distribution.ConfigGeneration"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RematerializationStatus": {
        "description": "RematerializationStatus is the state of one partition rebuild.",
        "properties": {
          "complete": {
            "description": "Complete is true once the rebuild has finished and the old mounts have been dropped.",
            "type": "boolean"
          },
          "documents": {
            "description": "Documents is how many documents have been written to the rebuild so far, and the size of the finished partition once it is done.",
            "type": "integer"
          },
          "language": {
            "type": "string"
          },
          "period": {
            "type": "string"
          },
          "phase": {
            "description": "Phase is the step the rebuild is at, and \"done\" when it has finished.",
            "type": "string"
          },
          "replaced": {
            "description": "Replaced are the mounted archive indexes the rebuild takes over from.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "revision": {
            "description": "Revision is which generation of the partition the rebuild produces, counting the copy the original archival produced as zero.",
            "type": "integer"
          },
          "started_at": {
            "description": "StartedAt and UpdatedAt bound the run, RFC3339.",
            "type": "string"
          },
          "trigger": {
            "description": "Trigger is what ordered the rebuild: \"manual\", \"overlay\", \"erasure\" or \"rollup\".",
            "type": "string"
          },
          "type": {
            "description": "Type, Language and Period identify the partition.",
            "type": "string"
          },
          "updated_at": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RematerializePartitionRequest": {
        "properties": {
          "language": {
            "description": "Language segment of the partition, e.g. \"sv\". This is the index language series, not a document language attribute.",
            "type": "string"
          },
          "period": {
            "description": "Period of the partition: \"yyyyq[1-4]\" for a quarter, or \"yyyy\" to roll a whole year of quarters into one partition.",
            "type": "string"
          },
          "type": {
            "description": "Type of the partition to rebuild, e.g. \"core/article\".",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RematerializePartitionResponse": {
        "properties": {
          "status": {
            "$ref": "#/components/schemas/elephant.distribution.RematerializationStatus"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RenditionConfiguration": {
        "description": "RenditionConfiguration configures delivery-time rendition generation for one asset kind.",
        "properties": {
          "default_extension": {
            "description": "DefaultExtension is the extension used when a request doesn't specify one.",
            "type": "string"
          },
          "default_variants": {
            "description": "DefaultVariants are the variants generated when a request doesn't specify any.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "kind": {
            "description": "Kind is the asset kind, e.g. \"image\".",
            "type": "string"
          },
          "sources": {
            "description": "Sources in evaluation order.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.RenditionSource"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RenditionParameters": {
        "description": "RenditionParameters selects the renditions to generate for one asset kind at delivery time.",
        "properties": {
          "extension": {
            "description": "Extension of the generated renditions, e.g. \"jpg\". Empty falls back to the kind's configured default extension.",
            "type": "string"
          },
          "variants": {
            "description": "Variants to generate rendition links for. Empty falls back to the kind's configured default variants.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.RenditionSource": {
        "description": "RenditionSource matches asset references in documents and maps them to an asset CDN namespace. Sources are evaluated in order; the first match wins.",
        "properties": {
          "block_types": {
            "description": "BlockTypes are the document block types to inspect. Optional, defaults per kind (\"core/image\" for the \"image\" kind).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "link_rel": {
            "description": "LinkRel is the block link rel that carries the asset reference. Optional, defaults per kind (\"image\" for the \"image\" kind).",
            "type": "string"
          },
          "link_types": {
            "description": "LinkTypes are the accepted types of the asset reference link.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "name": {
            "description": "Name identifies the source within its kind.",
            "type": "string"
          },
          "namespace": {
            "description": "Namespace is the asset CDN namespace to build rendition URLs in.",
            "type": "string"
          },
          "uri_pattern": {
            "description": "URIPattern is an anchored regular expression with exactly one capture group that extracts the asset ID from the link URI.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Schema": {
        "properties": {
          "name": {
            "type": "string"
          },
          "spec": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.SetSyncStateRequest": {
        "properties": {
          "desired": {
            "description": "Desired state of the sync worker, either \"running\" or \"paused\".",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.SetSyncStateResponse": {
        "type": "object"
      },
      "elephant.distribution.SigningKey": {
        "description": "SigningKey is the public half of one transparency-log signing key: the JWK the JWKS endpoint publishes, with its fields broken out.",
        "properties": {
          "curve": {
            "description": "Curve is the JWK \"crv\", always \"P-384\".",
            "type": "string"
          },
          "issued_at": {
            "description": "IssuedAt is when the key was created, in the canonical fixed-width RFC 3339 form the events use (\"2006-01-02T15:04:05.000000Z\", UTC).",
            "type": "string"
          },
          "key_id": {
            "description": "KeyID is what the second segment of a signature names.",
            "type": "string"
          },
          "key_type": {
            "description": "KeyType is the JWK \"kty\", always \"EC\".",
            "type": "string"
          },
          "not_after": {
            "type": "string"
          },
          "not_before": {
            "description": "NotBefore and NotAfter bound the window the key signs in. They are worth checking against an entry's created_at - a signature made outside its key's window verifies arithmetically but should not have existed - and they are why a key is published before it is used: a verifier needs it in hand before the first entry it signs arrives.",
            "type": "string"
          },
          "x": {
            "description": "X is the base64url-encoded (unpadded) big-endian x coordinate of the public point, 48 bytes decoded. As in a JWK, so a client can hand {kty, crv, x, y} straight to a JWK importer.",
            "type": "string"
          },
          "y": {
            "description": "Y is the y coordinate, same encoding.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.SimilarityPercentile": {
        "description": "SimilarityPercentile is one point of the similarity distribution.",
        "properties": {
          "percentile": {
            "description": "Percentile the value is for, e.g. 50, 99 or 99.9.",
            "type": "number"
          },
          "similarity": {
            "description": "Similarity at that percentile, on the same scale as VectorClauseMember.min_similarity.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Sort": {
        "description": "Sort sorts the hits on a field. Sort on keyword, date, boolean or numeric fields; text fields aren't sortable.",
        "properties": {
          "desc": {
            "description": "Desc sorts in descending order instead of ascending.",
            "type": "boolean"
          },
          "field": {
            "description": "Field to sort on.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.StringValues": {
        "description": "StringValues is the value of a flat index field. All field values are carried as strings regardless of the type of the field; dates are RFC 3339. A field is always a list because flattening a document collapses repeated blocks into parallel lists - values of the same block instance can't be paired up across fields.",
        "properties": {
          "values": {
            "description": "Values of the field, in index order.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Subscription": {
        "description": "Subscription is a stored subscription: its definition plus everything the server decided about it.",
        "properties": {
          "created_at": {
            "description": "CreatedAt is when the subscription was created, RFC3339.",
            "type": "string"
          },
          "created_by": {
            "description": "CreatedBy is the subject that created the subscription. Recorded for audit on organisation-owned subscriptions, where it does not confer ownership: any client of the organisation can manage and poll them.",
            "type": "string"
          },
          "deactivated_at": {
            "description": "DeactivatedAt is when the subscription stopped matching because its keep-alive ran out, RFC3339. Empty while it is active, and on a durable subscription, which never lapses. A deactivated subscription no longer costs anything at match time and is removed altogether once its match log has expired.",
            "type": "string"
          },
          "expires_at": {
            "description": "ExpiresAt is when the subscription is deactivated unless it is touched before then, RFC3339. Empty on a durable subscription.",
            "type": "string"
          },
          "id": {
            "description": "ID of the subscription, assigned by the server.",
            "type": "string"
          },
          "keep_alive_seconds": {
            "description": "KeepAliveSeconds is the time the subscription survives untouched. Zero on a durable subscription, which does not expire. Independent of the ownership tier: either tier can be given a keep-alive, and either can be durable.",
            "type": "integer"
          },
          "owner": {
            "description": "Owner is the organisation of the creator for an organisation-owned subscription, and the subject of the creator for a session subscription.",
            "type": "string"
          },
          "owner_tier": {
            "description": "OwnerTier is \"org\" for an organisation-owned subscription and \"subject\" for one owned by the subject that created it.",
            "type": "string"
          },
          "spec": {
            "$ref": "#/components/schemas/elephant.distribution.SubscriptionSpec"
          },
          "touched_at": {
            "description": "TouchedAt is when the subscription was last touched, RFC3339. Empty on a durable subscription, which has no clock to reset.",
            "type": "string"
          },
          "updated_at": {
            "description": "UpdatedAt is when the definition was last changed, RFC3339. Touches do not count as changes.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.SubscriptionMatch": {
        "description": "SubscriptionMatch is one document event that matched a subscription. Matches are recorded once per (subscription, event) and delivery is at-least-once, so a client can see the same event twice after a restart; the event ID is what makes it safe to deduplicate.",
        "properties": {
          "event_id": {
            "description": "EventID is the distribution eventlog ID of the event that matched. It is the cursor of the match log, and it is the same ID the event has in Content.GetNewDocuments.",
            "type": "integer"
          },
          "event_time": {
            "description": "EventTime is when distribution emitted the event, RFC3339. It is distribution's own clock, not the publication date: an event for historical content has an event_time of when it was ingested.",
            "type": "string"
          },
          "event_type": {
            "description": "EventType is either \"published\" or \"unpublished\".",
            "type": "string"
          },
          "matched_at": {
            "description": "MatchedAt is when the match was recorded, RFC3339. It normally trails event_time by seconds; a large gap means the match was made while catching up after an outage, and whether that is still worth alerting on is a decision for the client. Nothing is dropped for being late.",
            "type": "string"
          },
          "provenance": {
            "$ref": "#/components/schemas/elephant.distribution.MatchProvenance"
          },
          "published_at": {
            "description": "PublishedAt is when the version was published in the source system, RFC3339 - the publication date of the content, as opposed to event_time. It is the same value Content.GetNewDocuments reports as the event's published_at.",
            "type": "string"
          },
          "type": {
            "description": "Type of the document.",
            "type": "string"
          },
          "uuid": {
            "description": "UUID of the document. Pass it to Content.GetDocument to fetch the document itself.",
            "type": "string"
          },
          "version": {
            "description": "Version of the document that matched.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "elephant.distribution.SubscriptionSpec": {
        "description": "SubscriptionSpec is the definition of a subscription: everything about it that the client decides. Ownership, expiry and timestamps are not part of it, they are assigned by the server and read back on Subscription.",
        "properties": {
          "document_types": {
            "description": "DocumentTypes to evaluate against. Required, at least one: a subscription always names the types it is about. A type that isn't indexed is an invalid argument.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "language": {
            "description": "Language to match, as the language tag documents are indexed under, e.g. \"sv\" or \"sv-se\". Empty matches every language. Unlike a search query, which is per language series (see Search.Query), leaving the language out costs nothing here: matching runs per incoming document, so there is no fan-out.",
            "type": "string"
          },
          "max_age_seconds": {
            "description": "MaxAgeSeconds only matches documents first published within this many seconds of the event. Zero, the default, matches whatever the query and the clauses match, however old the document is. Set it on alert-style subscriptions: an update to a years-old article is a fresh event and would otherwise fire the alert.",
            "type": "integer"
          },
          "notify_once": {
            "description": "NotifyOnce suppresses re-notification for a document this subscription has already matched, whatever the later event on it is. Off by default, in which case a correction to a matched document is a new match with its own event type, which is usually what an editorial consumer wants. Composes with max_age_seconds rather than replacing it.",
            "type": "boolean"
          },
          "query": {
            "$ref": "#/components/schemas/elephant.distribution.Query"
          },
          "vector_clauses": {
            "description": "VectorClauses are the semantic part of the subscription. All of them have to be satisfied, and they are ANDed with the query. An empty list means the subscription is the query alone.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.VectorClause"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.Subsets": {
        "properties": {
          "expressions": {
            "description": "Expressions to use to extract document contents.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TermQuery": {
        "description": "TermQuery matches an exact, unanalysed value. Use it for keyword, boolean, date and numeric fields; a term query against a text field matches individual analysed tokens, which is rarely what you want.",
        "properties": {
          "boost": {
            "description": "Boost multiplies the score contribution of the clause. Defaults to 1.",
            "type": "number"
          },
          "field": {
            "description": "Field to match against.",
            "type": "string"
          },
          "value": {
            "description": "Value that the field must have.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TermsQuery": {
        "description": "TermsQuery matches documents where the field has any of the values.",
        "properties": {
          "boost": {
            "description": "Boost multiplies the score contribution of the clause. Defaults to 1.",
            "type": "number"
          },
          "field": {
            "description": "Field to match against.",
            "type": "string"
          },
          "values": {
            "description": "Values, any one of which is a match.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.ThresholdEstimate": {
        "description": "ThresholdEstimate is what one candidate threshold would have cost in volume over the calibration window.",
        "properties": {
          "matched_documents": {
            "description": "MatchedDocuments is the number of documents in the window that reached it.",
            "type": "integer"
          },
          "matches_per_day": {
            "description": "MatchesPerDay is matched_documents spread over the window. It is a rate over past traffic, not a forecast, and it is per clause: a subscription that ANDs this clause with a query or another clause will see fewer.",
            "type": "number"
          },
          "min_similarity": {
            "description": "MinSimilarity is the candidate threshold.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TouchSubscriptionRequest": {
        "properties": {
          "id": {
            "description": "ID of the subscription to touch. Required.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TouchSubscriptionResponse": {
        "properties": {
          "expires_at": {
            "description": "ExpiresAt is when the subscription is deactivated unless it is touched again before then, RFC3339. Empty on organisation-owned subscriptions.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TypeConfiguration": {
        "properties": {
          "anchor": {
            "description": "Anchor selects how documents of the type are partitioned across the search indexes. An empty value is a non-temporal type: one unpartitioned index per language that is never archived. \"first_published\" partitions by the time of the document's first distribution event, which is immutable, so a document never moves between partitions. \"time_expressions\" partitions by the dates time_expressions extracts from the document, with everything from the current quarter onwards sharing one index.",
            "type": "string"
          },
          "bounded_collection": {
            "description": "Set to true if this is a collection of documents that are bounded (limited in size). Bounded collections can be fetched by type and language in one go.",
            "type": "boolean"
          },
          "embeddings": {
            "description": "Embeddings enables semantic indexing of the document type: its documents are chunked and embedded, its indexes are created with a vector field, and it can be searched and subscribed to by vector. Independent of the anchor: a non-temporal type can be embedded, and a partitioned one can be left BM25-only.\n The vector field is set at index creation and cannot be added afterwards, so enabling this for a type that already has indexes takes a new index generation. Turning it off leaves the field in place, unwritten. It also needs a deployment with an embedding sidecar; without one the type is indexed BM25-only whatever this says.",
            "type": "boolean"
          },
          "facet_expressions": {
            "description": "FacetExpressions extract the values the daily views of Content.ListPublishedVersions and Content.ListPlannedVersions can narrow on. They are evaluated on the store path, per version, and the result is stored with the version - so a facet only narrows the versions that were written after it was configured, and adding one means backfilling what is already stored.\n Extraction is configuration rather than code because a facet is not in the same place in every type: a flash, an article and a planning item all reference their section differently.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.TypeFacetExpression"
            },
            "type": "array"
          },
          "label_expressions": {
            "description": "LabelExpressions are used to extract labels from documents.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.LabelExpression"
            },
            "type": "array"
          },
          "time_expressions": {
            "description": "TimeExpressions are used for extracting time ranges from documents.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.TypeTimeExpression"
            },
            "type": "array"
          },
          "transform_script": {
            "description": "TransformScript is the transformation script for this document type.",
            "type": "string"
          },
          "variants": {
            "description": "Variants are the allowed variants for the document type.",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TypeFacetExpression": {
        "description": "TypeFacetExpression extracts one facet's values from a document.",
        "properties": {
          "expression": {
            "description": "Expression is a newsdoc value extraction expression. It should yield document UUIDs rather than labels - a facet filter matches exactly, with no analysis or case folding, and the client resolves display names itself.",
            "type": "string"
          },
          "name": {
            "description": "Name of the facet the extracted values belong to. Several expressions can share a name, and their values are unioned.\n The vocabulary is deliberately small; \"section\" is the one facet the daily views are built for.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.TypeTimeExpression": {
        "properties": {
          "expression": {
            "description": "Expression is a newsdoc value extraction expression.",
            "type": "string"
          },
          "layout": {
            "description": "Layout is the time/date format to use when parsing. Optional, defaults to RFC3339 or ISO 8601 for values annotated as dates.",
            "type": "string"
          },
          "timezone": {
            "description": "Timezone is the timezone the time should be parsed in. Optional, most timestamps should include timezone information, if they don't, parsing will fall back to the default timezone that the repository has been configured with.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "elephant.distribution.UpdateSubscriptionRequest": {
        "properties": {
          "id": {
            "description": "ID of the subscription to update. Required.",
            "type": "string"
          },
          "keep_alive_seconds": {
            "description": "KeepAliveSeconds sets a new keep-alive, clamped to at most seven days. Absent leaves the current one alone; zero removes it and makes the subscription durable. Applies to both ownership tiers. The ownership tier itself cannot be changed.",
            "type": "integer"
          },
          "subscription": {
            "$ref": "#/components/schemas/elephant.distribution.SubscriptionSpec"
          }
        },
        "type": "object"
      },
      "elephant.distribution.UpdateSubscriptionResponse": {
        "properties": {
          "subscription": {
            "$ref": "#/components/schemas/elephant.distribution.Subscription"
          }
        },
        "type": "object"
      },
      "elephant.distribution.VectorClause": {
        "description": "VectorClause is one semantic condition of a subscription. Its members are ORed: the clause is satisfied when any of them passes, or, when the clause is negated, when none of them does. Several conditions are several clauses, and they are ANDed - \"close to A or B, but not about C\" is two clauses, one with A and B as members and one negated clause with C.",
        "properties": {
          "members": {
            "description": "Members of the clause, at least one. Each carries its own threshold, so an OR can be built out of sources that are not equally easy to be close to.",
            "items": {
              "$ref": "#/components/schemas/elephant.distribution.VectorClauseMember"
            },
            "type": "array"
          },
          "negate": {
            "description": "Negate inverts the clause: it is satisfied when no member passes, i.e. when the document is close to none of them. Use PreviewSubscription before trusting a negated clause - what it excludes cannot be seen in what it returns.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "elephant.distribution.VectorClauseMember": {
        "description": "VectorClauseMember is one vector to compare documents against, with the similarity at which it counts as a match. Exactly one source must be set.\n A member passes when the document has an embedding at or above min_similarity: the document is scored as the highest similarity across its chunk embeddings, so a long document matches on the part of it that is about the member.\n Prefer text and exemplars over a raw vector. Both are stored with the source and the embedding model they were derived under, which is what lets the server re-embed them and recalibrate their thresholds when the model is upgraded. A raw vector cannot be re-derived, and a member that holds one is paused and flagged for the owner instead.",
        "properties": {
          "exemplars": {
            "$ref": "#/components/schemas/elephant.distribution.ExemplarDocuments"
          },
          "min_similarity": {
            "description": "MinSimilarity is the cosine similarity at or above which this member passes, in [-1, 1], where higher means closer and 1 means identical direction. The embeddings are normalized, so this is the same dial as a maximum inner product distance, and it is the same number as Hit.score for a knn query and KnnQuery.min_similarity - which is how a threshold is found without guessing, see CalibrateVectorClause and Search.Query.\n Required: absent is an invalid argument. The field is optional so that a threshold of 0 - close to anything that isn't pointing away - is distinguishable from not stating one.\n Thresholds are model-dependent and are not portable between deployments or across an embedding model upgrade. Treat one as something to recalibrate, not as a constant.",
            "type": "number"
          },
          "model": {
            "description": "Model is the identifier of the embedding model the stored vector of this member was derived under. Set by the server on read, ignored on write.",
            "type": "string"
          },
          "text": {
            "description": "Text to be close to, embedded server-side with the current model. The normal choice: a phrase or a paragraph describing the subject matter of the subscription.",
            "type": "string"
          },
          "vector": {
            "$ref": "#/components/schemas/elephant.distribution.RawVector"
          }
        },
        "type": "object"
      },
      "newsdoc.Block": {
        "description": "Block is the building block for data embedded in documents. It is used for both content, links and metadata. Blocks have can be nested, but that's nothing to strive for, keep it simple.",
        "properties": {
          "content": {
            "description": "Content is used to embed content blocks.",
            "items": {
              "$ref": "#/components/schemas/newsdoc.Block"
            },
            "type": "array"
          },
          "contenttype": {
            "description": "ContentType is used to describe the content type of the block/linked entity if it differs from the type of the block.",
            "type": "string"
          },
          "data": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Data contains block data.",
            "type": "object"
          },
          "id": {
            "description": "ID is the block ID,",
            "type": "string"
          },
          "links": {
            "description": "Links are used to link to other resources and documents.",
            "items": {
              "$ref": "#/components/schemas/newsdoc.Block"
            },
            "type": "array"
          },
          "meta": {
            "description": "Meta is used to embed metadata",
            "items": {
              "$ref": "#/components/schemas/newsdoc.Block"
            },
            "type": "array"
          },
          "name": {
            "description": "Name is a name for the block. An alternative to \"rel\" when relationship is a term that doesn't fit.",
            "type": "string"
          },
          "rel": {
            "description": "Rel describes the relationship to the document/parent entity.",
            "type": "string"
          },
          "role": {
            "description": "Role is used either as an alternative to rel, or for nuancing the relationship.",
            "type": "string"
          },
          "sensitivity": {
            "description": "Sensitivity can be use to communicate how the information in a block can be handled. It could f.ex. be set to \"internal\", to show that it contains information that must be removed or transformed before publishing.",
            "type": "string"
          },
          "title": {
            "description": "Title is the title/headline of the block, typically used in the presentation of the block.",
            "type": "string"
          },
          "type": {
            "description": "Type is the type of the block",
            "type": "string"
          },
          "uri": {
            "description": "URI is used to reference another entity in a document.",
            "type": "string"
          },
          "url": {
            "description": "URL is a browseable URL for the the block.",
            "type": "string"
          },
          "uuid": {
            "description": "UUID is used to reference another Document in a block.",
            "type": "string"
          },
          "value": {
            "description": "Value is a value for the block. Useful when we want to store a primitive value.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "newsdoc.Document": {
        "description": "Document is a NewsDoc document.",
        "properties": {
          "content": {
            "description": "Content is the content of the document, this is essentially what gets rendered on the page when you view a document.",
            "items": {
              "$ref": "#/components/schemas/newsdoc.Block"
            },
            "type": "array"
          },
          "language": {
            "description": "Language is the language used in the document as an IETF language tag. F.ex. \"en\", \"en-UK\", \"es\", or \"sv-SE\".",
            "type": "string"
          },
          "links": {
            "description": "Links are links to other resources and entities. This could be links to topics, categories and subjects for the document, or credited authors.",
            "items": {
              "$ref": "#/components/schemas/newsdoc.Block"
            },
            "type": "array"
          },
          "meta": {
            "description": "Meta is the metadata for a document, this could be things like teasers, open graph data, newsvalues.",
            "items": {
              "$ref": "#/components/schemas/newsdoc.Block"
            },
            "type": "array"
          },
          "title": {
            "description": "Title is the title of the document, can be used as the document name, or the headline when the document is displayed.",
            "type": "string"
          },
          "type": {
            "description": "Type is the content type of the document.",
            "type": "string"
          },
          "uri": {
            "description": "URI identifies the document (in a more human-readable way than the UUID).",
            "type": "string"
          },
          "url": {
            "description": "URL is the browseable location of the document (if any).",
            "type": "string"
          },
          "uuid": {
            "description": "UUID is a unique ID for the document, this can for example be a random v4 UUID, or a URI-derived v5 UUID.",
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearer": {
        "bearerFormat": "JWT",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "title": "distribution API",
    "version": "v0.0.7"
  },
  "openapi": "3.0.0",
  "paths": {
    "/twirp/elephant.distribution.Archive/GetArchivedDocument": {
      "post": {
        "description": "GetArchivedDocument returns a fresh presigned URL for a specific archived document version. Used to resolve replay bodies and to re-mint a URL a consumer let expire.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetArchivedDocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetArchivedDocumentResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetArchivedDocument",
        "tags": [
          "Archive"
        ]
      }
    },
    "/twirp/elephant.distribution.Archive/ListArchiveChunks": {
      "post": {
        "description": "ListArchiveChunks lists archived eventlog chunks in order, for full ordered replay of the history that precedes the hot log.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListArchiveChunksRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListArchiveChunksResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListArchiveChunks",
        "tags": [
          "Archive"
        ]
      }
    },
    "/twirp/elephant.distribution.Archive/ListArchivedDocuments": {
      "post": {
        "description": "ListArchivedDocuments enumerates the latest archived version of every document offloaded to the archive, for a current-state snapshot. Pair it with a Content.GetNewDocuments tail from watermark_event_id to reconstruct full current state.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListArchivedDocumentsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListArchivedDocumentsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListArchivedDocuments",
        "tags": [
          "Archive"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/ActivateConfigGeneration": {
      "post": {
        "description": "ActivateConfigGeneration switches the active configuration to the generation with the given ID.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ActivateConfigGenerationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ActivateConfigGenerationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ActivateConfigGeneration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/ActivateIndexGeneration": {
      "post": {
        "description": "ActivateIndexGeneration makes a generation the one that search, threshold calibration and subscription matching read. The switch is atomic and exactly one generation is active at a time.\n Activation is refused when the candidate lags the head of the eventlog by more than a small gate, ten events by default, because a generation that is behind would answer with stale content the moment it took over. It is refused on the same gate when the candidate stands that far above the position the outgoing generation has matched up to, since that distance is what the matching handover has to replay and an outgoing generation that is stalled or disabled can be arbitrarily far behind a candidate that is on the head. Force activates regardless, which is the right call when the outgoing generation is unreachable and the wrong one otherwise.\n Subscription matching is handed over rather than dropped. Activation records the position the outgoing generation had matched up to and stops its matching there; the incoming generation replays the events from that position up to its own cursor for matching only. Match rows are keyed on subscription and event, so replaying an event is idempotent - but the two generations read the log in different pages, and a document that occurs more than once in a page is matched under the last event of that page, so the same document version can end up recorded under two events. Delivery across a cutover is at-least-once with the occasional duplicate: deduplicate on the subscription, the document UUID and the version if that matters to you.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ActivateIndexGenerationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ActivateIndexGenerationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ActivateIndexGeneration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/CreateIndexGeneration": {
      "post": {
        "description": "CreateIndexGeneration registers a new index generation: a complete set of search indexes with an index name prefix of its own, built on one of the OpenSearch clusters the deployment declares.\n The generation is populated by the ordinary backfill - the indexer that picks it up drains the archive and then tails the eventlog - while the active generation keeps serving. Subscription matching is suppressed for the whole build, so creating a generation never re-notifies a subscription, but the percolator indexes are seeded as it goes, so matching works from the moment it is activated.\n Use it to rebuild the index from scratch after a mapping change that cannot be applied to live indexes, or to move the index to another cluster - which is how an OpenSearch major version upgrade is done: create a generation on the new cluster, watch it catch up with ListIndexGenerations, compare results through the generation override on Search.Query, activate, and delete the old one.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.CreateIndexGenerationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.CreateIndexGenerationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "CreateIndexGeneration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/DeleteIndexGeneration": {
      "post": {
        "description": "DeleteIndexGeneration deletes an inactive generation. The active generation cannot be deleted - activate another one first.\n The call records the deletion and returns; the cleanup runs asynchronously and is scoped by the generation's index prefix: its document, percolator and overlay indexes, the bookkeeping rows that name them, its eventlog cursor, and the snapshots under the generation's base path in the snapshot repository. Nothing outside the prefix is touched, so deleting a generation can never take content away from the one that is serving.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.DeleteIndexGenerationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.DeleteIndexGenerationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "DeleteIndexGeneration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/EraseDocument": {
      "post": {
        "description": "EraseDocument erases the content of a document from every store that holds it: the stored document versions, the archived document objects and their vector cache, the search index copies, and the placement and subscription bookkeeping that names it.\n The eventlog is untouched. Its entries carry identifiers and hashes only, never document content, so the transparency log survives an erasure with its hash chain intact.\n Erasure is point-in-time. It removes the content that exists when it is ordered and does not blocklist the UUID: a document re-published afterwards is a new document and is distributed and indexed normally.\n The call is idempotent and returns the recorded request. A request is complete once no archived index snapshot holds a copy any more, which can require the affected partitions to be re-materialized - use GetErasureStatus to prove completion.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.EraseDocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.EraseDocumentResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "EraseDocument",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetActiveConfigGeneration": {
      "post": {
        "description": "GetActiveConfigGeneration returns the currently active generation, including its schemas and type configurations. Supports long polling for changes.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetActiveConfigGenerationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetActiveConfigGenerationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetActiveConfigGeneration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetAllActiveSchemas": {
      "post": {
        "description": "GetAllActiveSchemas returns the schemas in the active generation. Supports long polling so clients can watch for changes.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetAllActiveSchemasRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetAllActiveSchemasResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetAllActiveSchemas",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetDocumentTypes": {
      "post": {
        "description": "GetDocumentTypes lists the document types declared by schemas in the active generation.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetDocumentTypesRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetDocumentTypesResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetDocumentTypes",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetErasureStatus": {
      "post": {
        "description": "GetErasureStatus returns the recorded erasure request for a document, including the archived partitions that are still awaiting re-materialization.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetErasureStatusRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetErasureStatusResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetErasureStatus",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetSchema": {
      "post": {
        "description": "GetSchema retrieves a stored schema spec by name and version.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetSchemaRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetSchemaResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetSchema",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetSyncStatus": {
      "post": {
        "description": "GetSyncStatus returns the desired and current state of the distribution sync worker together with its position in the repository eventlog.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetSyncStatusRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetSyncStatusResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetSyncStatus",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/GetTypeConfiguration": {
      "post": {
        "description": "GetTypeConfiguration returns the configuration for a type in the active generation.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetTypeConfigurationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetTypeConfigurationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetTypeConfiguration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/ListConfigGenerations": {
      "post": {
        "description": "ListConfigGenerations lists historical generations, most recent first.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListConfigGenerationsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListConfigGenerationsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListConfigGenerations",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/ListIndexGenerations": {
      "post": {
        "description": "ListIndexGenerations lists the index generations together with the position each one's indexer has reached in the distribution eventlog and the current head of that log. That is the catch-up view a rebuild is followed through: a candidate is ready to be activated once its lag has come down to a handful of events.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListIndexGenerationsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListIndexGenerationsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListIndexGenerations",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/RegisterConfigGeneration": {
      "post": {
        "description": "RegisterConfigGeneration registers a complete configuration set (schemas + type configurations) as a new generation. Existing schema versions can be re-used by name and version without re-supplying the spec; if the spec is supplied for an existing version it must match the stored one. Returns the new generation ID.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.RegisterConfigGenerationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.RegisterConfigGenerationResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "RegisterConfigGeneration",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/RematerializePartition": {
      "post": {
        "description": "RematerializePartition orders an archived index partition to be rebuilt from the archive: overlay corrections are folded in, deleted and erased documents are left out, and the result is built with the current mappings and schemas.\n The call records the order and returns; the rebuild itself runs in the index tiering job, which owns the snapshot repository and the warm tier. Call it again to see how far it has got - the response is the recorded state either way, and ordering a rebuild that is already under way changes nothing.\n A period of the form \"yyyy\" orders the rollup of a whole year: the years quarter partitions are rebuilt into a single yearly one and their mounts are dropped.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.RematerializePartitionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.RematerializePartitionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "RematerializePartition",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Configuration/SetSyncState": {
      "post": {
        "description": "SetSyncState sets the desired state of the distribution sync worker. A cold-started service (no configuration, no sync position) starts out paused so that the repository eventlog isn't consumed - and discarded - before a configuration generation has been activated.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.SetSyncStateRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.SetSyncStateResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "SetSyncState",
        "tags": [
          "Configuration"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/CancelImport": {
      "post": {
        "description": "CancelImport cancels an import. Cancellation is terminal: the job stops being claimable by the loader, the imports queued behind it proceed, and its hold on index maintenance is released. An un-finalized upload can be cancelled too, which retires its upload ID.\n Only a job that is not actively loading can be cancelled: a load that keeps failing backs off between passes with status \"failed\", which is when a cancel lands. Documents the job already published are signed log entries and stay published - remediation of loaded content is per-document erasure, not cancellation.\n Requires dist_admin.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.CancelImportRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.CancelImportResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "CancelImport",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/CreateImportUpload": {
      "post": {
        "description": "CreateImportUpload starts a bulk import upload: the client declares the files that make up the import set and gets a presigned PUT URL back for each of them. The files are uploaded directly to object storage; the service only brokers the URLs. Files are gzipped NDJSON with one import envelope per line, and they are loaded in the order they were declared, so declare them in chronological (quarterly) order.\n To resume an upload that was cut short, call CreateImportUpload again with the upload ID and the same file declaration: fresh URLs are returned for every file, and only the ones that are missing or were interrupted need to be PUT again. Re-uploading a completed file is harmless - finalization verifies sizes and hashes, not upload counts. An upload that is never finalized is eventually cleaned up.\n Requires dist_admin, unlike the service's dist_read baseline, and fails with FailedPrecondition when the deployment has no archive bucket configured - there is nowhere to upload to.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.CreateImportUploadRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.CreateImportUploadResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "CreateImportUpload",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/FinalizeImportUpload": {
      "post": {
        "description": "FinalizeImportUpload declares an upload complete and turns it into an import job. The service verifies that every declared file is present in the bucket with the declared size and sha256 - a missing or mismatching file is a FailedPrecondition, and the upload stays open so the file can be re-uploaded and finalization retried - records the import, and starts loading asynchronously. The returned import ID is what GetImportStatus takes; the upload ID is consumed and invalid afterwards.\n Loaded documents go through the same store path as live content: document versions are upserted and the events enter the transparency log hashed and signed like any others, carrying the import's source so that consumers can tell bulk-imported content from the live repository flow. The load is resumable and idempotent per document version - a service restart continues where it left off.\n Requires dist_admin and an archive bucket, as CreateImportUpload.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.FinalizeImportUploadRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.FinalizeImportUploadResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "FinalizeImportUpload",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/GetDocument": {
      "post": {
        "description": "GetDocument returns a single document, transparently falling back to the archive if the requested version (or the whole document) has been evicted from the hot data set. The document is returned in-band.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetDocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetDocumentResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetDocument",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/GetDocumentVersions": {
      "post": {
        "description": "GetDocumentVersions loads a batch of document versions. It is the batch form of GetDocument, and it is how a client streams in the documents a listing left deferred.\n Responses that carry documents bound how many they carry, because a document whose body has aged out of the hot store is an archive read each and loading a whole page of them would make the response time a function of how far back the caller browsed. Entries beyond that bound come back with deferred set and no document; pass their UUID and version here to fetch them, in whatever order and at whatever rate suits the client - typically what is on screen first, and the rest as the reader scrolls.\n The same bound applies here: ask for more than the server will load in one response and the surplus comes back deferred again, so a client that keeps requesting what it did not get makes progress rather than looping.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetDocumentVersionsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetDocumentVersionsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetDocumentVersions",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/GetImportStatus": {
      "post": {
        "description": "GetImportStatus returns the recorded state of an import job: where the load has got to, and what went wrong if it failed.\n Requires dist_admin.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetImportStatusRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetImportStatusResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetImportStatus",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/GetNewDocuments": {
      "post": {
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetNewDocumentsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetNewDocumentsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetNewDocuments",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/GetSigningKeys": {
      "post": {
        "description": "GetSigningKeys returns the public keys the transparency log's signatures are made with, so that a consumer reading the log with GetNewDocumentsRequest.verification can verify it without leaving the API it is already talking to.\n The same keys are served as a JWKS at the unauthenticated GET /signing-keys and mirrored into the archive bucket. Those remain the way an independent verifier with no credentials, checking archived content, gets them. This is the convenience for a consumer that already holds a client.\n An independent copy is worth keeping either way: keys fetched from the service that signed the log only prove it is internally consistent.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetSigningKeysRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetSigningKeysResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetSigningKeys",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/ListImports": {
      "post": {
        "description": "ListImports lists import jobs, most recent first.\n Requires dist_admin.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListImportsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListImportsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListImports",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/ListPlannedVersions": {
      "post": {
        "description": "ListPlannedVersions lists what is planned for an interval, earliest first: the current version of every document of the requested types whose resolved anchor date falls in it.\n Only current versions, deliberately. A planning item's revisions are working state rather than publications, and a calendar showing five revisions of one item is noise - so unlike ListPublishedVersions this never carries version history. An item rescheduled out of the interval leaves it entirely rather than staying behind under its old date.\n Intended for forward-anchored types: planning items and calendar entries.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListPlannedVersionsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListPlannedVersionsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListPlannedVersions",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Content/ListPublishedVersions": {
      "post": {
        "description": "ListPublishedVersions lists the publication events of an interval, newest first: what was published, in the order it happened. Every stored version of every document of the requested types whose publication time falls in the interval, superseded ones included and marked as such, so that a correction sits at the minute it was made rather than under the document's latest version.\n This is the same log GetNewDocuments serves, read by publication time instead of by event ID. It is a range scan over the stored versions, not a search: the inputs are document types, an interval and facet values, the order is chronological, and there is no relevance, free-text or cross-type ranking to be had.\n Any interval can be listed, however old: retention drops document bodies and keeps the bookkeeping the listing is made of. Past inline_horizon the entries come back deferred and their documents are streamed in with GetDocumentVersions.\n Intended for backward-anchored types - articles, flashes, editorial information - where the question is what was published when. See ListPlannedVersions for the calendar view of forward-anchored types.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListPublishedVersionsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListPublishedVersionsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListPublishedVersions",
        "tags": [
          "Content"
        ]
      }
    },
    "/twirp/elephant.distribution.Search/GetMappings": {
      "post": {
        "description": "GetMappings returns the searchable fields of a document type and the type of each field, as the union across the language indexes. Use it to find out what can be queried, and how, before building a query.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetMappingsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetMappingsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetMappings",
        "tags": [
          "Search"
        ]
      }
    },
    "/twirp/elephant.distribution.Search/Query": {
      "post": {
        "description": "Query searches one language series of one tier. Language is required and a query never fans out across languages: searching several languages is several calls.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.QueryRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.QueryResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Query",
        "tags": [
          "Search"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/CalibrateVectorClause": {
      "post": {
        "description": "CalibrateVectorClause is how a min_similarity gets chosen. It scores the given clause members against the documents of a recent window and returns the similarity distribution, the top hits to eyeball, and - the number the decision is actually made on - the estimated matches per day at a ladder of candidate thresholds. \"At 0.62 this clause alone would have matched about 14 documents a day last week\" is what a threshold means in practice; a suggested threshold is a volume target, never a claim about relevance.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.CalibrateVectorClauseRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.CalibrateVectorClauseResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "CalibrateVectorClause",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/CreateSubscription": {
      "post": {
        "description": "CreateSubscription registers a new subscription and returns it as stored, with the ownership and expiry the server assigned to it. Matching starts at the next indexed document; documents that were indexed before the call are never matched, use PreviewSubscription to see what the subscription would have caught.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.CreateSubscriptionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.CreateSubscriptionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "CreateSubscription",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/DeleteSubscription": {
      "post": {
        "description": "DeleteSubscription removes a subscription and its match log. Deleting is permanent and distinct from a subscription expiring: polling a deleted subscription is a not-found, polling an expired one is a failed precondition.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.DeleteSubscriptionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.DeleteSubscriptionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "DeleteSubscription",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/GetSubscription": {
      "post": {
        "description": "GetSubscription returns a subscription as stored. Counts as a touch.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.GetSubscriptionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.GetSubscriptionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "GetSubscription",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/ListSubscriptions": {
      "post": {
        "description": "ListSubscriptions lists the subscriptions the caller can see: the ones owned by the organisation of the caller, and the ones owned by the calling subject. Listing does not count as a touch, so it is safe to poll for housekeeping without keeping abandoned subscriptions alive.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.ListSubscriptionsRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.ListSubscriptionsResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "ListSubscriptions",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/PollMatches": {
      "post": {
        "description": "PollMatches reads the match log of a subscription, long-polling for new matches. Delivery is at-least-once and ordered by event ID, which doubles as the cursor: pass the highest event ID you have processed as after_event_id on the next call.\n Polling a subscription that has expired is a failed precondition, not a not-found: the subscription was abandoned rather than deleted, and the client can tell the two apart and re-create if it wants to. Counts as a touch, so a client that keeps polling never expires.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.PollMatchesRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.PollMatchesResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "PollMatches",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/PreviewSubscription": {
      "post": {
        "description": "PreviewSubscription is the dry run of a complete subscription definition against a recent window: what would have matched, with the same provenance the match log carries. It is the acceptance test for a new subscription, and the only practical way to reason about a negated clause, which is invisible in the results it does not exclude.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.PreviewSubscriptionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.PreviewSubscriptionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "PreviewSubscription",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/TouchSubscription": {
      "post": {
        "description": "TouchSubscription resets the keep-alive clock of a subscription without reading anything else. Ordinary use does not need it - PollMatches, GetSubscription and UpdateSubscription all count as touches - it is for clients that hold a subscription open while they are not polling. Touching a durable subscription is a no-op, it has no clock to reset.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.TouchSubscriptionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.TouchSubscriptionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "TouchSubscription",
        "tags": [
          "Subscriptions"
        ]
      }
    },
    "/twirp/elephant.distribution.Subscriptions/UpdateSubscription": {
      "post": {
        "description": "UpdateSubscription replaces the definition of a subscription. The spec is written whole: fields left out of it are cleared, not kept. Vector clause members are re-embedded and the query part is re-translated, so an update takes effect for documents indexed after it returns. Counts as a touch.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/elephant.distribution.UpdateSubscriptionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/elephant.distribution.UpdateSubscriptionResponse"
                }
              }
            },
            "description": "Method response"
          },
          "default": {
            "description": ""
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "UpdateSubscription",
        "tags": [
          "Subscriptions"
        ]
      }
    }
  },
  "servers": [
    {
      "url": "https://distribution.api.tt.se"
    },
    {
      "url": "https://distribution.api.stage.tt.se"
    }
  ],
  "tags": [
    {
      "name": "Configuration"
    },
    {
      "name": "Content"
    },
    {
      "description": "Archive exposes the offloaded eventlog history and document versions for bulk export - e.g. building or rebuilding a search index over the full content set. Bodies are delivered as presigned URLs that the consumer fetches directly from object storage; the service only brokers the catalog and mints the URLs. Object keys and the presigning mechanism are internal: consumers address content by identity (uuid, version, chunk_id), never by storage key.",
      "name": "Archive"
    },
    {
      "description": "Search exposes the distribution search index: a query surface over the documents that Content delivers, with the same visibility rules. It is read-only.\n Access requires the \"dist_search\" scope, which is granted by \"dist_read\" (search is a view of what the caller can already fetch) and by \"dist_admin\".",
      "name": "Search"
    },
    {
      "description": "Subscriptions are standing queries. Instead of polling Search for whatever is new, a client registers what it is interested in once, and the indexer evaluates every document it indexes against the registration. Matches land in a per-subscription log that the client reads with PollMatches; matching latency is indexing latency, a few seconds behind publication.\n A document matches when the query part matches, if there is one, and every vector clause is satisfied. Clauses are ANDed, the members of a clause are ORed, and a negated clause is satisfied when none of its members pass. OR across a text query and a vector clause is deliberately not expressible - that is two subscriptions.\n Ownership and expiry are two separate choices. The owner is never taken from the request - it is the organisation claim or the subject of the credentials the call was made with - and owner_tier picks which of the two, defaulting to the organisation. A keep-alive, independently of that, makes the subscription expire when it goes untouched for that long; without one it lives until it is deleted. See CreateSubscriptionRequest.\n Access requires the \"dist_subscribe\" scope, which is granted by \"dist_admin\".",
      "name": "Subscriptions"
    }
  ]
}